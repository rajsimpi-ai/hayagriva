{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Hayagriva","text":"<p>Hayagriva is a lightweight, modular Retrieval-Augmented Generation framework. It connects document ingestion, chunking, embeddings, vector retrieval, and Groq generation behind a small Python API.</p>"},{"location":"#what-hayagriva-supports","title":"What Hayagriva Supports","text":"<ul> <li>Groq generation backend</li> <li>FAISS local vector search</li> <li>Weaviate vector, BM25, and hybrid retrieval</li> <li>Word, recursive, semantic, and hierarchical chunking</li> <li>File and directory ingestion for <code>.txt</code> documents</li> <li>Lower-level building blocks for ingestion, chunking, indexing, and retrieval</li> </ul>"},{"location":"#quick-example","title":"Quick Example","text":"<pre><code>from hayagriva import Hayagriva, HayagrivaConfig\n\nrag = Hayagriva(\n    HayagrivaConfig(\n        backend=\"groq\",\n        api_key=\"YOUR_GROQ_KEY\",\n        model=\"llama-3.1-8b-instant\",\n    )\n)\n\nrag.add_documents([\"Hayagriva restores forgotten knowledge.\"])\nanswer = rag.ask(\"What does Hayagriva restore?\")\nprint(answer)\n</code></pre>"},{"location":"#where-to-go-next","title":"Where To Go Next","text":"<ul> <li>Start with Installation.</li> <li>Learn the full flow in Basic RAG.</li> <li>Explore focused workflows in Examples.</li> <li>See available objects in API Reference.</li> </ul>"},{"location":"examples/","title":"Examples","text":"<p>The examples are organized by what part of the pipeline they demonstrate.</p>"},{"location":"examples/#ingestion","title":"Ingestion","text":"<p>Load raw strings or <code>.txt</code> files:</p> <ul> <li><code>examples/ingestion/load_texts.py</code></li> <li><code>examples/ingestion/load_from_files.py</code></li> </ul>"},{"location":"examples/#chunking","title":"Chunking","text":"<p>Run chunkers directly:</p> <ul> <li><code>examples/chunking/word_chunking.py</code></li> <li><code>examples/chunking/recursive_chunking.py</code></li> <li><code>examples/chunking/semantic_chunking.py</code></li> <li><code>examples/chunking/hierarchical_chunking.py</code></li> </ul>"},{"location":"examples/#indexing","title":"Indexing","text":"<p>Embed chunks and store them:</p> <ul> <li><code>examples/indexing/embed_texts.py</code></li> <li><code>examples/indexing/faiss_index_chunks.py</code></li> <li><code>examples/indexing/weaviate_index_chunks.py</code></li> </ul>"},{"location":"examples/#retrieval","title":"Retrieval","text":"<p>Retrieve chunks without generation:</p> <ul> <li><code>examples/retrieval/faiss_retrieve_chunks.py</code></li> <li><code>examples/retrieval/weaviate_retrieve_chunks.py</code></li> </ul>"},{"location":"examples/#end-to-end","title":"End To End","text":"<p>Full RAG flows:</p> <ul> <li><code>examples/end_to_end/basic_rag_groq_faiss.py</code></li> <li><code>examples/end_to_end/rag_with_metadata.py</code></li> <li><code>examples/end_to_end/rag_with_file_ingestion.py</code></li> <li><code>examples/end_to_end/custom_chunking_and_retrieval.py</code></li> <li><code>examples/end_to_end/custom_embedding_model.py</code></li> </ul>"},{"location":"examples/#weaviate","title":"Weaviate","text":"<p>Weaviate-specific retrieval modes:</p> <ul> <li><code>examples/vectorstores/weaviate/vector_search.py</code></li> <li><code>examples/vectorstores/weaviate/bm25_search.py</code></li> <li><code>examples/vectorstores/weaviate/hybrid_search.py</code></li> </ul>"},{"location":"getting-started/configuration/","title":"Configuration","text":"<p>Hayagriva is configured with <code>HayagrivaConfig</code> and smaller focused config objects.</p>"},{"location":"getting-started/configuration/#main-configuration","title":"Main Configuration","text":"<pre><code>from hayagriva import HayagrivaConfig\n\nconfig = HayagrivaConfig(\n    backend=\"groq\",\n    api_key=\"YOUR_GROQ_KEY\",\n    model=\"llama-3.1-8b-instant\",\n    vector_store=\"faiss\",\n)\n</code></pre>"},{"location":"getting-started/configuration/#chunking-configuration","title":"Chunking Configuration","text":"<pre><code>from hayagriva.config import ChunkingConfig\n\nchunking = ChunkingConfig(\n    strategy=\"recursive\",\n    chunk_size=500,\n    overlap=50,\n)\n</code></pre> <p>Supported strategies:</p> <ul> <li><code>word</code></li> <li><code>recursive</code></li> <li><code>semantic</code></li> <li><code>hierarchical</code></li> </ul>"},{"location":"getting-started/configuration/#retrieval-configuration","title":"Retrieval Configuration","text":"<pre><code>from hayagriva.config import RetrievalConfig\n\nretrieval = RetrievalConfig(\n    strategy=\"vector\",\n    top_k=4,\n    similarity_threshold=0.0,\n)\n</code></pre> <p>Supported strategies:</p> <ul> <li><code>vector</code>: FAISS and Weaviate</li> <li><code>bm25</code>: Weaviate</li> <li><code>hybrid</code>: Weaviate</li> </ul>"},{"location":"getting-started/configuration/#weaviate-configuration","title":"Weaviate Configuration","text":"<pre><code>from hayagriva.config import WeaviateConfig\n\nweaviate = WeaviateConfig(\n    url=\"http://localhost:8080\",\n    index_name=\"HayagrivaDocs\",\n)\n</code></pre> <p>For hosted Weaviate:</p> <pre><code>weaviate = WeaviateConfig(\n    url=\"https://your-cluster.weaviate.network\",\n    api_key=\"YOUR_WEAVIATE_API_KEY\",\n    index_name=\"HayagrivaDocs\",\n)\n</code></pre>"},{"location":"getting-started/installation/","title":"Installation","text":"<p>Hayagriva requires Python 3.10 or newer.</p>"},{"location":"getting-started/installation/#install-from-pypi","title":"Install From PyPI","text":"<pre><code>pip install hayagriva\n</code></pre>"},{"location":"getting-started/installation/#local-faiss-setup","title":"Local FAISS Setup","text":"<p>For local examples that use sentence-transformers and FAISS:</p> <pre><code>pip install \"hayagriva[cpu]\"\n</code></pre> <p>For development from this repository:</p> <pre><code>bash setup/install_local_deps.sh\n</code></pre>"},{"location":"getting-started/installation/#weaviate-setup","title":"Weaviate Setup","text":"<p>Weaviate examples need the local dependencies and a running Weaviate instance:</p> <pre><code>bash setup/install_local_deps.sh\nbash setup/start_weaviate.sh\n</code></pre> <p>Then set the default local URL:</p> <pre><code>export WEAVIATE_URL=\"http://localhost:8080\"\n</code></pre> <p>Stop the local Weaviate service with:</p> <pre><code>bash setup/stop_weaviate.sh\n</code></pre>"},{"location":"getting-started/installation/#groq-api-key","title":"Groq API Key","text":"<p>End-to-end generation examples require a Groq API key:</p> <pre><code>export GROQ_API_KEY=\"your-groq-api-key\"\n</code></pre>"},{"location":"getting-started/installation/#documentation-setup","title":"Documentation Setup","text":"<p>To work on these docs locally:</p> <pre><code>pip install \".[docs]\"\nmkdocs serve\n</code></pre> <p>Build the static site with:</p> <pre><code>mkdocs build\n</code></pre>"},{"location":"guides/basic-rag/","title":"Basic RAG","text":"<p>This guide shows the full Hayagriva flow:</p> <ol> <li>Configure the backend.</li> <li>Add documents.</li> <li>Retrieve relevant chunks.</li> <li>Generate an answer with Groq.</li> </ol> <pre><code>from hayagriva import Hayagriva, HayagrivaConfig\n\nconfig = HayagrivaConfig(\n    backend=\"groq\",\n    api_key=\"YOUR_GROQ_KEY\",\n    model=\"llama-3.1-8b-instant\",\n)\n\nrag = Hayagriva(config)\nrag.add_documents(\n    [\n        \"Hayagriva is associated with restored knowledge.\",\n        \"RAG systems retrieve context before generating an answer.\",\n        \"FAISS is useful for local vector search.\",\n    ]\n)\n\nanswer = rag.ask(\"Why does RAG retrieve context first?\")\nprint(answer)\n</code></pre>"},{"location":"guides/basic-rag/#return-metadata","title":"Return Metadata","text":"<p>Use <code>return_metadata=True</code> to inspect retrieval and model details:</p> <pre><code>response = rag.ask(\n    \"Why does RAG retrieve context first?\",\n    return_metadata=True,\n)\n\nprint(response[\"answer\"])\nprint(response[\"chunks\"])\nprint(response[\"retrieval\"])\n</code></pre> <p>See <code>examples/end_to_end/</code> for complete runnable scripts.</p>"},{"location":"guides/chunking/","title":"Chunking","text":"<p>Chunking controls how source text is split before embedding and indexing.</p>"},{"location":"guides/chunking/#word-chunking","title":"Word Chunking","text":"<pre><code>from hayagriva.config import ChunkingConfig\nfrom hayagriva.core.chunker import WordChunker\n\nchunker = WordChunker(ChunkingConfig(chunk_size=100, overlap=20))\nchunks, metadata = chunker.chunk([\"Your document text here.\"])\n</code></pre>"},{"location":"guides/chunking/#recursive-chunking","title":"Recursive Chunking","text":"<p>Recursive chunking prefers larger separators first, then falls back to smaller ones.</p> <pre><code>from hayagriva.config import ChunkingConfig\nfrom hayagriva.core.chunker import RecursiveChunker\n\nchunker = RecursiveChunker(\n    ChunkingConfig(strategy=\"recursive\", chunk_size=300)\n)\nchunks, metadata = chunker.chunk([document])\n</code></pre>"},{"location":"guides/chunking/#semantic-chunking","title":"Semantic Chunking","text":"<p>Semantic chunking uses embeddings to split where adjacent sentence similarity drops.</p> <pre><code>from hayagriva.config import ChunkingConfig\nfrom hayagriva.core.chunker import SemanticChunker\nfrom hayagriva.core.embeddings import SentenceTransformerEmbeddings\n\nembedder = SentenceTransformerEmbeddings()\nchunker = SemanticChunker(\n    ChunkingConfig(strategy=\"semantic\", semantic_threshold=0.7),\n    embedder=embedder,\n)\nchunks, metadata = chunker.chunk([document])\n</code></pre>"},{"location":"guides/chunking/#hierarchical-chunking","title":"Hierarchical Chunking","text":"<p>Hierarchical chunking creates child chunks with parent context in metadata.</p> <pre><code>from hayagriva.config import ChunkingConfig\nfrom hayagriva.core.chunker import HierarchicalChunker\n\nchunker = HierarchicalChunker(\n    ChunkingConfig(\n        strategy=\"hierarchical\",\n        parent_chunk_size=1000,\n        chunk_size=200,\n        overlap=20,\n    )\n)\nchunks, metadata = chunker.chunk([document])\nprint(metadata[0][\"parent_text\"])\n</code></pre>"},{"location":"guides/ingestion/","title":"Ingestion","text":"<p>Hayagriva can ingest raw strings or load <code>.txt</code> files from paths.</p>"},{"location":"guides/ingestion/#load-raw-text","title":"Load Raw Text","text":"<pre><code>from hayagriva.ingestion.loaders import load_texts\n\ndocuments = load_texts([\n    \"Hayagriva restores knowledge.\",\n    \"RAG starts with clean source text.\",\n])\n</code></pre>"},{"location":"guides/ingestion/#load-files","title":"Load Files","text":"<pre><code>from hayagriva.ingestion.loaders import load_from_paths\n\ndocuments = load_from_paths([\"./docs\"])\n</code></pre> <p>Directory paths are searched recursively for <code>.txt</code> files.</p> <p>Runnable examples:</p> <ul> <li><code>examples/ingestion/load_texts.py</code></li> <li><code>examples/ingestion/load_from_files.py</code></li> </ul>"},{"location":"guides/vectorstores/","title":"Vector Stores","text":"<p>Hayagriva currently supports FAISS and Weaviate.</p>"},{"location":"guides/vectorstores/#faiss","title":"FAISS","text":"<p>FAISS is the default vector store and is useful for local experiments.</p> <pre><code>from hayagriva.core.vectorstores import FaissVectorStore\n\nstore = FaissVectorStore()\nstore.add(embeddings, chunks, metadata=metadata)\nresults = store.search(query_embedding, top_k=4)\n</code></pre>"},{"location":"guides/vectorstores/#weaviate","title":"Weaviate","text":"<p>Weaviate supports vector, BM25, and hybrid retrieval.</p> <pre><code>from hayagriva.config import WeaviateConfig\nfrom hayagriva.core.vectorstores import WeaviateVectorStore\n\nstore = WeaviateVectorStore(\n    WeaviateConfig(\n        url=\"http://localhost:8080\",\n        index_name=\"HayagrivaDocs\",\n    )\n)\nstore.add(embeddings, chunks, metadata=metadata)\n</code></pre> <p>Start a local Weaviate service with:</p> <pre><code>bash setup/start_weaviate.sh\n</code></pre> <p>Runnable examples:</p> <ul> <li><code>examples/indexing/faiss_index_chunks.py</code></li> <li><code>examples/indexing/weaviate_index_chunks.py</code></li> <li><code>examples/retrieval/faiss_retrieve_chunks.py</code></li> <li><code>examples/retrieval/weaviate_retrieve_chunks.py</code></li> <li><code>examples/vectorstores/weaviate/</code></li> </ul>"},{"location":"reference/api/","title":"API Reference","text":"<p>This page summarizes the main public objects. The source code includes detailed docstrings for classes, methods, arguments, return values, and raised errors.</p>"},{"location":"reference/api/#top-level-api","title":"Top-Level API","text":""},{"location":"reference/api/#hayagriva.Hayagriva","title":"<code>hayagriva.Hayagriva</code>","text":"<p>High-level interface for ingestion, retrieval, and generation.</p> <p><code>Hayagriva</code> wires together the configured embedder, chunker, vector store, retriever, and Groq generator. Use lower-level classes directly when you only need part of the pipeline, such as chunking or indexing.</p> <p>Parameters:</p> Name Type Description Default <code>config</code> <code>Optional[HayagrivaConfig]</code> <p>Optional top-level configuration. Defaults are Groq generation, FAISS vector storage, word chunking, and vector retrieval.</p> <code>None</code> <p>Attributes:</p> Name Type Description <code>config</code> <p>Effective Hayagriva configuration.</p> <code>embedder</code> <p>Sentence-Transformers embedding provider.</p> <code>chunker</code> <p>Chunker selected by <code>config.chunking.strategy</code>.</p> <code>vector_store</code> <p>Vector store selected by <code>config.vector_store</code>.</p> <code>retriever</code> <p>Retriever combining the embedder and vector store.</p> <code>generator</code> <p>Groq generator used by <code>ask</code>.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the backend, vector store, or chunking strategy is not supported.</p> <code>MissingDependencyError</code> <p>If a selected optional dependency is missing.</p> <code>GenerationError</code> <p>If the Groq API key is missing.</p> Source code in <code>hayagriva/core/hayagriva.py</code> <pre><code>class Hayagriva:\n    \"\"\"High-level interface for ingestion, retrieval, and generation.\n\n    ``Hayagriva`` wires together the configured embedder, chunker, vector store,\n    retriever, and Groq generator. Use lower-level classes directly when you\n    only need part of the pipeline, such as chunking or indexing.\n\n    Args:\n        config: Optional top-level configuration. Defaults are Groq generation,\n            FAISS vector storage, word chunking, and vector retrieval.\n\n    Attributes:\n        config: Effective Hayagriva configuration.\n        embedder: Sentence-Transformers embedding provider.\n        chunker: Chunker selected by ``config.chunking.strategy``.\n        vector_store: Vector store selected by ``config.vector_store``.\n        retriever: Retriever combining the embedder and vector store.\n        generator: Groq generator used by ``ask``.\n\n    Raises:\n        ValueError: If the backend, vector store, or chunking strategy is not\n            supported.\n        MissingDependencyError: If a selected optional dependency is missing.\n        GenerationError: If the Groq API key is missing.\n    \"\"\"\n\n    def __init__(self, config: Optional[HayagrivaConfig] = None) -&gt; None:\n        \"\"\"Initialize the configured RAG pipeline components.\n\n        Args:\n            config: Optional top-level configuration.\n\n        Raises:\n            ValueError: If an unsupported backend or vector store is selected.\n            MissingDependencyError: If required optional dependencies are\n                missing.\n            GenerationError: If Groq configuration is invalid.\n        \"\"\"\n        self.config = config or HayagrivaConfig()\n\n        # Core components\n        self.embedder = SentenceTransformerEmbeddings(\n            self.config.models.embedding_model\n        )\n\n        # Initialize Chunker based on strategy\n        strategy = self.config.chunking.strategy\n        if strategy == \"recursive\":\n            self.chunker = RecursiveChunker(self.config.chunking)\n        elif strategy == \"semantic\":\n            self.chunker = SemanticChunker(self.config.chunking, embedder=self.embedder)\n        elif strategy == \"hierarchical\":\n            self.chunker = HierarchicalChunker(self.config.chunking)\n        else:\n            self.chunker = WordChunker(self.config.chunking)\n\n        if self.config.vector_store == \"weaviate\":\n            self.vector_store = WeaviateVectorStore(self.config.weaviate)\n        elif self.config.vector_store == \"faiss\":\n            self.vector_store = FaissVectorStore()\n        else:\n            raise ValueError(f\"Unknown vector store: {self.config.vector_store}\")\n\n        self.retriever = Retriever(\n            self.embedder, self.vector_store, self.config.retrieval\n        )\n\n        # Backend selection\n        if self.config.backend == \"groq\":\n            from hayagriva.core.generators import GroqGenerator\n\n            self.generator = GroqGenerator(\n                api_key=self.config.api_key,\n                model=self.config.model,\n            )\n\n        else:\n            raise ValueError(f\"Unknown backend: {self.config.backend}\")\n\n        self._documents: List[str] = []\n\n    def add_documents(self, documents: Iterable[str]) -&gt; None:\n        \"\"\"Ingest documents into the configured vector store.\n\n        The method validates raw documents, chunks them with the configured\n        chunker, embeds each chunk, and stores the results through the retriever.\n\n        Args:\n            documents: Iterable of non-empty document strings.\n\n        Raises:\n            IngestionError: If no valid document text is provided.\n            MissingDependencyError: If embedding or vector-store dependencies\n                are missing.\n            ValueError: If vector-store inputs are inconsistent.\n        \"\"\"\n        texts = ensure_texts(documents)\n        self._documents.extend(texts)\n\n        chunks, metadata = self.chunker.chunk(texts)\n        logger.info(\"Chunked %d documents into %d chunks\", len(texts), len(chunks))\n\n        self.retriever.add(chunks, metadata)\n\n    def ask(self, question: str, return_metadata: bool = False) -&gt; Union[str, Dict[str, Any]]:\n        \"\"\"Answer a question using retrieval + generation.\n\n        Args:\n            question: Natural-language question to answer.\n            return_metadata: When ``True``, return a structured dictionary with\n                the answer plus retrieval, chunking, and model metadata.\n\n        Returns:\n            Generated answer text when ``return_metadata`` is ``False``. When\n            ``return_metadata`` is ``True``, returns a dictionary with ``answer``,\n            ``question``, ``chunks``, ``retrieval``, ``chunking``, and ``model``.\n\n        Raises:\n            GenerationError: If the configured generator fails.\n            ConfigurationError: If retrieval configuration is invalid.\n        \"\"\"\n\n        results = self.retriever.retrieve(question)\n        contexts = [chunk for chunk, _ in results]\n\n        context_block = build_context(contexts)\n        prompt = build_prompt(question, contexts)\n\n        logger.info(\"Built prompt with context length %d\", len(context_block))\n\n        result = self.generator.generate(prompt)\n\n        if return_metadata:\n            if hasattr(result, \"__iter__\") and not isinstance(result, str):\n                answer = \"\".join(list(result))\n            else:\n                answer = result\n\n            chunks = [\n                {\"rank\": idx + 1, \"text\": chunk, \"score\": score}\n                for idx, (chunk, score) in enumerate(results)\n            ]\n\n            return {\n                \"answer\": answer,\n                \"question\": question,\n                \"chunks\": chunks,\n                \"retrieval\": {\n                    \"strategy\": self.config.retrieval.strategy,\n                    \"top_k\": self.config.retrieval.top_k,\n                    \"similarity_threshold\": self.config.retrieval.similarity_threshold,\n                    \"alpha\": self.config.retrieval.alpha,\n                },\n                \"chunking\": {\n                    \"strategy\": self.config.chunking.strategy,\n                    \"chunk_size\": self.config.chunking.chunk_size,\n                    \"overlap\": self.config.chunking.overlap,\n                },\n                \"model\": {\n                    \"backend\": self.config.backend,\n                    \"model\": self.config.model,\n                    \"embedding_model\": self.config.models.embedding_model,\n                    \"vector_store\": self.config.vector_store,\n                },\n            }\n\n        return result\n\n    def get_index_size(self) -&gt; int:\n        \"\"\"Return the number of indexed chunks when the store exposes it.\n\n        Returns:\n            Number of chunks in stores that expose a ``chunks`` attribute, or\n            ``0`` for stores where local size is not available.\n        \"\"\"\n        if hasattr(self.vector_store, \"chunks\"):\n            return len(self.vector_store.chunks)\n        return 0\n\n    @property\n    def documents(self) -&gt; List[str]:\n        \"\"\"Return a copy of raw documents added through ``add_documents``.\n\n        Returns:\n            List of original document strings retained by the facade.\n        \"\"\"\n        return list(self._documents)\n</code></pre>"},{"location":"reference/api/#hayagriva.Hayagriva.documents","title":"<code>documents</code>  <code>property</code>","text":"<p>Return a copy of raw documents added through <code>add_documents</code>.</p> <p>Returns:</p> Type Description <code>List[str]</code> <p>List of original document strings retained by the facade.</p>"},{"location":"reference/api/#hayagriva.Hayagriva.__init__","title":"<code>__init__(config=None)</code>","text":"<p>Initialize the configured RAG pipeline components.</p> <p>Parameters:</p> Name Type Description Default <code>config</code> <code>Optional[HayagrivaConfig]</code> <p>Optional top-level configuration.</p> <code>None</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>If an unsupported backend or vector store is selected.</p> <code>MissingDependencyError</code> <p>If required optional dependencies are missing.</p> <code>GenerationError</code> <p>If Groq configuration is invalid.</p> Source code in <code>hayagriva/core/hayagriva.py</code> <pre><code>def __init__(self, config: Optional[HayagrivaConfig] = None) -&gt; None:\n    \"\"\"Initialize the configured RAG pipeline components.\n\n    Args:\n        config: Optional top-level configuration.\n\n    Raises:\n        ValueError: If an unsupported backend or vector store is selected.\n        MissingDependencyError: If required optional dependencies are\n            missing.\n        GenerationError: If Groq configuration is invalid.\n    \"\"\"\n    self.config = config or HayagrivaConfig()\n\n    # Core components\n    self.embedder = SentenceTransformerEmbeddings(\n        self.config.models.embedding_model\n    )\n\n    # Initialize Chunker based on strategy\n    strategy = self.config.chunking.strategy\n    if strategy == \"recursive\":\n        self.chunker = RecursiveChunker(self.config.chunking)\n    elif strategy == \"semantic\":\n        self.chunker = SemanticChunker(self.config.chunking, embedder=self.embedder)\n    elif strategy == \"hierarchical\":\n        self.chunker = HierarchicalChunker(self.config.chunking)\n    else:\n        self.chunker = WordChunker(self.config.chunking)\n\n    if self.config.vector_store == \"weaviate\":\n        self.vector_store = WeaviateVectorStore(self.config.weaviate)\n    elif self.config.vector_store == \"faiss\":\n        self.vector_store = FaissVectorStore()\n    else:\n        raise ValueError(f\"Unknown vector store: {self.config.vector_store}\")\n\n    self.retriever = Retriever(\n        self.embedder, self.vector_store, self.config.retrieval\n    )\n\n    # Backend selection\n    if self.config.backend == \"groq\":\n        from hayagriva.core.generators import GroqGenerator\n\n        self.generator = GroqGenerator(\n            api_key=self.config.api_key,\n            model=self.config.model,\n        )\n\n    else:\n        raise ValueError(f\"Unknown backend: {self.config.backend}\")\n\n    self._documents: List[str] = []\n</code></pre>"},{"location":"reference/api/#hayagriva.Hayagriva.add_documents","title":"<code>add_documents(documents)</code>","text":"<p>Ingest documents into the configured vector store.</p> <p>The method validates raw documents, chunks them with the configured chunker, embeds each chunk, and stores the results through the retriever.</p> <p>Parameters:</p> Name Type Description Default <code>documents</code> <code>Iterable[str]</code> <p>Iterable of non-empty document strings.</p> required <p>Raises:</p> Type Description <code>IngestionError</code> <p>If no valid document text is provided.</p> <code>MissingDependencyError</code> <p>If embedding or vector-store dependencies are missing.</p> <code>ValueError</code> <p>If vector-store inputs are inconsistent.</p> Source code in <code>hayagriva/core/hayagriva.py</code> <pre><code>def add_documents(self, documents: Iterable[str]) -&gt; None:\n    \"\"\"Ingest documents into the configured vector store.\n\n    The method validates raw documents, chunks them with the configured\n    chunker, embeds each chunk, and stores the results through the retriever.\n\n    Args:\n        documents: Iterable of non-empty document strings.\n\n    Raises:\n        IngestionError: If no valid document text is provided.\n        MissingDependencyError: If embedding or vector-store dependencies\n            are missing.\n        ValueError: If vector-store inputs are inconsistent.\n    \"\"\"\n    texts = ensure_texts(documents)\n    self._documents.extend(texts)\n\n    chunks, metadata = self.chunker.chunk(texts)\n    logger.info(\"Chunked %d documents into %d chunks\", len(texts), len(chunks))\n\n    self.retriever.add(chunks, metadata)\n</code></pre>"},{"location":"reference/api/#hayagriva.Hayagriva.ask","title":"<code>ask(question, return_metadata=False)</code>","text":"<p>Answer a question using retrieval + generation.</p> <p>Parameters:</p> Name Type Description Default <code>question</code> <code>str</code> <p>Natural-language question to answer.</p> required <code>return_metadata</code> <code>bool</code> <p>When <code>True</code>, return a structured dictionary with the answer plus retrieval, chunking, and model metadata.</p> <code>False</code> <p>Returns:</p> Type Description <code>Union[str, Dict[str, Any]]</code> <p>Generated answer text when <code>return_metadata</code> is <code>False</code>. When</p> <code>Union[str, Dict[str, Any]]</code> <p><code>return_metadata</code> is <code>True</code>, returns a dictionary with <code>answer</code>,</p> <code>Union[str, Dict[str, Any]]</code> <p><code>question</code>, <code>chunks</code>, <code>retrieval</code>, <code>chunking</code>, and <code>model</code>.</p> <p>Raises:</p> Type Description <code>GenerationError</code> <p>If the configured generator fails.</p> <code>ConfigurationError</code> <p>If retrieval configuration is invalid.</p> Source code in <code>hayagriva/core/hayagriva.py</code> <pre><code>def ask(self, question: str, return_metadata: bool = False) -&gt; Union[str, Dict[str, Any]]:\n    \"\"\"Answer a question using retrieval + generation.\n\n    Args:\n        question: Natural-language question to answer.\n        return_metadata: When ``True``, return a structured dictionary with\n            the answer plus retrieval, chunking, and model metadata.\n\n    Returns:\n        Generated answer text when ``return_metadata`` is ``False``. When\n        ``return_metadata`` is ``True``, returns a dictionary with ``answer``,\n        ``question``, ``chunks``, ``retrieval``, ``chunking``, and ``model``.\n\n    Raises:\n        GenerationError: If the configured generator fails.\n        ConfigurationError: If retrieval configuration is invalid.\n    \"\"\"\n\n    results = self.retriever.retrieve(question)\n    contexts = [chunk for chunk, _ in results]\n\n    context_block = build_context(contexts)\n    prompt = build_prompt(question, contexts)\n\n    logger.info(\"Built prompt with context length %d\", len(context_block))\n\n    result = self.generator.generate(prompt)\n\n    if return_metadata:\n        if hasattr(result, \"__iter__\") and not isinstance(result, str):\n            answer = \"\".join(list(result))\n        else:\n            answer = result\n\n        chunks = [\n            {\"rank\": idx + 1, \"text\": chunk, \"score\": score}\n            for idx, (chunk, score) in enumerate(results)\n        ]\n\n        return {\n            \"answer\": answer,\n            \"question\": question,\n            \"chunks\": chunks,\n            \"retrieval\": {\n                \"strategy\": self.config.retrieval.strategy,\n                \"top_k\": self.config.retrieval.top_k,\n                \"similarity_threshold\": self.config.retrieval.similarity_threshold,\n                \"alpha\": self.config.retrieval.alpha,\n            },\n            \"chunking\": {\n                \"strategy\": self.config.chunking.strategy,\n                \"chunk_size\": self.config.chunking.chunk_size,\n                \"overlap\": self.config.chunking.overlap,\n            },\n            \"model\": {\n                \"backend\": self.config.backend,\n                \"model\": self.config.model,\n                \"embedding_model\": self.config.models.embedding_model,\n                \"vector_store\": self.config.vector_store,\n            },\n        }\n\n    return result\n</code></pre>"},{"location":"reference/api/#hayagriva.Hayagriva.get_index_size","title":"<code>get_index_size()</code>","text":"<p>Return the number of indexed chunks when the store exposes it.</p> <p>Returns:</p> Type Description <code>int</code> <p>Number of chunks in stores that expose a <code>chunks</code> attribute, or</p> <code>int</code> <p><code>0</code> for stores where local size is not available.</p> Source code in <code>hayagriva/core/hayagriva.py</code> <pre><code>def get_index_size(self) -&gt; int:\n    \"\"\"Return the number of indexed chunks when the store exposes it.\n\n    Returns:\n        Number of chunks in stores that expose a ``chunks`` attribute, or\n        ``0`` for stores where local size is not available.\n    \"\"\"\n    if hasattr(self.vector_store, \"chunks\"):\n        return len(self.vector_store.chunks)\n    return 0\n</code></pre>"},{"location":"reference/api/#hayagriva.HayagrivaConfig","title":"<code>hayagriva.HayagrivaConfig</code>","text":"<p>Top-level configuration for the Hayagriva facade.</p> <p>Parameters:</p> Name Type Description Default <code>backend</code> <code>str</code> <p>Generation backend. Currently only <code>\"groq\"</code> is supported.</p> <code>'groq'</code> <code>api_key</code> <code>Optional[str]</code> <p>API key for the selected generation backend.</p> <code>None</code> <code>model</code> <code>str</code> <p>Generation model name passed to the backend.</p> <code>'llama-3.1-8b-instant'</code> <code>embedding_model</code> <code>str</code> <p>Sentence-Transformers model used when <code>models</code> is not provided.</p> <code>'all-MiniLM-L6-v2'</code> <code>vector_store</code> <code>str</code> <p>Vector store backend. Supported values are <code>\"faiss\"</code> and <code>\"weaviate\"</code>.</p> <code>'faiss'</code> <code>weaviate</code> <code>Optional[WeaviateConfig]</code> <p>Optional Weaviate-specific configuration.</p> <code>None</code> <code>chunking</code> <code>Optional[ChunkingConfig]</code> <p>Optional chunking configuration.</p> <code>None</code> <code>models</code> <code>Optional[ModelConfig]</code> <p>Optional model configuration. When provided, this takes precedence over <code>embedding_model</code>.</p> <code>None</code> <code>retrieval</code> <code>Optional[RetrievalConfig]</code> <p>Optional retrieval configuration.</p> <code>None</code> <p>Attributes:</p> Name Type Description <code>backend</code> <p>Selected generation backend name.</p> <code>api_key</code> <p>API key for the backend.</p> <code>model</code> <p>Generation model name.</p> <code>vector_store</code> <p>Selected vector store backend name.</p> <code>chunking</code> <p>Chunking configuration instance.</p> <code>models</code> <p>Model configuration instance.</p> <code>retrieval</code> <p>Retrieval configuration instance.</p> <code>weaviate</code> <p>Weaviate configuration instance.</p> Source code in <code>hayagriva/config.py</code> <pre><code>class HayagrivaConfig:\n    \"\"\"Top-level configuration for the Hayagriva facade.\n\n    Args:\n        backend: Generation backend. Currently only ``\"groq\"`` is supported.\n        api_key: API key for the selected generation backend.\n        model: Generation model name passed to the backend.\n        embedding_model: Sentence-Transformers model used when ``models`` is\n            not provided.\n        vector_store: Vector store backend. Supported values are ``\"faiss\"``\n            and ``\"weaviate\"``.\n        weaviate: Optional Weaviate-specific configuration.\n        chunking: Optional chunking configuration.\n        models: Optional model configuration. When provided, this takes\n            precedence over ``embedding_model``.\n        retrieval: Optional retrieval configuration.\n\n    Attributes:\n        backend: Selected generation backend name.\n        api_key: API key for the backend.\n        model: Generation model name.\n        vector_store: Selected vector store backend name.\n        chunking: Chunking configuration instance.\n        models: Model configuration instance.\n        retrieval: Retrieval configuration instance.\n        weaviate: Weaviate configuration instance.\n    \"\"\"\n\n    def __init__(\n        self,\n        backend: str = \"groq\",\n        api_key: Optional[str] = None,\n        model: str = \"llama-3.1-8b-instant\",\n        embedding_model: str = \"all-MiniLM-L6-v2\",\n        vector_store: str = \"faiss\",\n        weaviate: Optional[WeaviateConfig] = None,\n        chunking: Optional[ChunkingConfig] = None,\n        models: Optional[ModelConfig] = None,\n        retrieval: Optional[RetrievalConfig] = None,\n    ) -&gt; None:\n        \"\"\"Create a top-level Hayagriva configuration object.\n\n        Args:\n            backend: Generation backend name.\n            api_key: API key for the selected generation backend.\n            model: Generation model name.\n            embedding_model: Embedding model name used when ``models`` is not\n                provided.\n            vector_store: Vector store backend name.\n            weaviate: Optional Weaviate configuration.\n            chunking: Optional chunking configuration.\n            models: Optional model configuration.\n            retrieval: Optional retrieval configuration.\n        \"\"\"\n        self.backend = backend        # \"groq\"\n        self.api_key = api_key        # API key for chosen backend\n        self.model = model            # model name for backend\n        self.vector_store = vector_store # \"faiss\" or \"weaviate\"\n\n        # Existing config objects\n        self.chunking = chunking or ChunkingConfig()\n        self.models = models or ModelConfig(embedding_model=embedding_model)\n        self.retrieval = retrieval or RetrievalConfig()\n        self.weaviate = weaviate or WeaviateConfig()\n</code></pre>"},{"location":"reference/api/#hayagriva.HayagrivaConfig.__init__","title":"<code>__init__(backend='groq', api_key=None, model='llama-3.1-8b-instant', embedding_model='all-MiniLM-L6-v2', vector_store='faiss', weaviate=None, chunking=None, models=None, retrieval=None)</code>","text":"<p>Create a top-level Hayagriva configuration object.</p> <p>Parameters:</p> Name Type Description Default <code>backend</code> <code>str</code> <p>Generation backend name.</p> <code>'groq'</code> <code>api_key</code> <code>Optional[str]</code> <p>API key for the selected generation backend.</p> <code>None</code> <code>model</code> <code>str</code> <p>Generation model name.</p> <code>'llama-3.1-8b-instant'</code> <code>embedding_model</code> <code>str</code> <p>Embedding model name used when <code>models</code> is not provided.</p> <code>'all-MiniLM-L6-v2'</code> <code>vector_store</code> <code>str</code> <p>Vector store backend name.</p> <code>'faiss'</code> <code>weaviate</code> <code>Optional[WeaviateConfig]</code> <p>Optional Weaviate configuration.</p> <code>None</code> <code>chunking</code> <code>Optional[ChunkingConfig]</code> <p>Optional chunking configuration.</p> <code>None</code> <code>models</code> <code>Optional[ModelConfig]</code> <p>Optional model configuration.</p> <code>None</code> <code>retrieval</code> <code>Optional[RetrievalConfig]</code> <p>Optional retrieval configuration.</p> <code>None</code> Source code in <code>hayagriva/config.py</code> <pre><code>def __init__(\n    self,\n    backend: str = \"groq\",\n    api_key: Optional[str] = None,\n    model: str = \"llama-3.1-8b-instant\",\n    embedding_model: str = \"all-MiniLM-L6-v2\",\n    vector_store: str = \"faiss\",\n    weaviate: Optional[WeaviateConfig] = None,\n    chunking: Optional[ChunkingConfig] = None,\n    models: Optional[ModelConfig] = None,\n    retrieval: Optional[RetrievalConfig] = None,\n) -&gt; None:\n    \"\"\"Create a top-level Hayagriva configuration object.\n\n    Args:\n        backend: Generation backend name.\n        api_key: API key for the selected generation backend.\n        model: Generation model name.\n        embedding_model: Embedding model name used when ``models`` is not\n            provided.\n        vector_store: Vector store backend name.\n        weaviate: Optional Weaviate configuration.\n        chunking: Optional chunking configuration.\n        models: Optional model configuration.\n        retrieval: Optional retrieval configuration.\n    \"\"\"\n    self.backend = backend        # \"groq\"\n    self.api_key = api_key        # API key for chosen backend\n    self.model = model            # model name for backend\n    self.vector_store = vector_store # \"faiss\" or \"weaviate\"\n\n    # Existing config objects\n    self.chunking = chunking or ChunkingConfig()\n    self.models = models or ModelConfig(embedding_model=embedding_model)\n    self.retrieval = retrieval or RetrievalConfig()\n    self.weaviate = weaviate or WeaviateConfig()\n</code></pre>"},{"location":"reference/api/#configuration","title":"Configuration","text":""},{"location":"reference/api/#hayagriva.config.ModelConfig","title":"<code>hayagriva.config.ModelConfig</code>  <code>dataclass</code>","text":"<p>Model settings used by Hayagriva.</p> <p>Attributes:</p> Name Type Description <code>embedding_model</code> <code>str</code> <p>Sentence-Transformers model name used to embed documents, chunks, and queries.</p> Source code in <code>hayagriva/config.py</code> <pre><code>@dataclass\nclass ModelConfig:\n    \"\"\"Model settings used by Hayagriva.\n\n    Attributes:\n        embedding_model: Sentence-Transformers model name used to embed\n            documents, chunks, and queries.\n    \"\"\"\n\n    embedding_model: str = \"all-MiniLM-L6-v2\"\n</code></pre>"},{"location":"reference/api/#hayagriva.config.ChunkingConfig","title":"<code>hayagriva.config.ChunkingConfig</code>  <code>dataclass</code>","text":"<p>Configuration shared by text chunking strategies.</p> <p>Attributes:</p> Name Type Description <code>chunk_size</code> <code>int</code> <p>Target chunk size. Word-based chunkers interpret this as a word count.</p> <code>overlap</code> <code>int</code> <p>Number of words to carry from one word chunk into the next.</p> <code>strategy</code> <code>str</code> <p>Chunking strategy name. Supported values are <code>\"word\"</code>, <code>\"recursive\"</code>, <code>\"semantic\"</code>, and <code>\"hierarchical\"</code>.</p> <code>separators</code> <code>list</code> <p>Ordered separators used by the recursive chunker.</p> <code>semantic_threshold</code> <code>float</code> <p>Minimum adjacent-sentence cosine similarity used by semantic chunking before starting a new chunk.</p> <code>parent_chunk_size</code> <code>int</code> <p>Parent chunk size used by hierarchical chunking.</p> Source code in <code>hayagriva/config.py</code> <pre><code>@dataclass\nclass ChunkingConfig:\n    \"\"\"Configuration shared by text chunking strategies.\n\n    Attributes:\n        chunk_size: Target chunk size. Word-based chunkers interpret this as a\n            word count.\n        overlap: Number of words to carry from one word chunk into the next.\n        strategy: Chunking strategy name. Supported values are ``\"word\"``,\n            ``\"recursive\"``, ``\"semantic\"``, and ``\"hierarchical\"``.\n        separators: Ordered separators used by the recursive chunker.\n        semantic_threshold: Minimum adjacent-sentence cosine similarity used by\n            semantic chunking before starting a new chunk.\n        parent_chunk_size: Parent chunk size used by hierarchical chunking.\n    \"\"\"\n\n    chunk_size: int = 200\n    overlap: int = 20\n    strategy: str = \"word\"  # \"word\", \"recursive\", \"semantic\", \"hierarchical\"\n    separators: list = field(default_factory=lambda: [\"\\n\\n\", \"\\n\", \" \", \"\"])\n    semantic_threshold: float = 0.8\n    parent_chunk_size: int = 1000\n</code></pre>"},{"location":"reference/api/#hayagriva.config.RetrievalConfig","title":"<code>hayagriva.config.RetrievalConfig</code>  <code>dataclass</code>","text":"<p>Configuration for retriever search behavior.</p> <p>Attributes:</p> Name Type Description <code>top_k</code> <code>int</code> <p>Maximum number of chunks to return for a query.</p> <code>similarity_threshold</code> <code>float</code> <p>Minimum score required for a result to be kept.</p> <code>strategy</code> <code>str</code> <p>Retrieval strategy. FAISS supports <code>\"vector\"</code>; Weaviate supports <code>\"vector\"</code>, <code>\"bm25\"</code>, and <code>\"hybrid\"</code>.</p> <code>alpha</code> <code>float</code> <p>Hybrid retrieval weight for Weaviate. <code>0.0</code> emphasizes sparse BM25 matching and <code>1.0</code> emphasizes dense vector matching.</p> Source code in <code>hayagriva/config.py</code> <pre><code>@dataclass\nclass RetrievalConfig:\n    \"\"\"Configuration for retriever search behavior.\n\n    Attributes:\n        top_k: Maximum number of chunks to return for a query.\n        similarity_threshold: Minimum score required for a result to be kept.\n        strategy: Retrieval strategy. FAISS supports ``\"vector\"``; Weaviate\n            supports ``\"vector\"``, ``\"bm25\"``, and ``\"hybrid\"``.\n        alpha: Hybrid retrieval weight for Weaviate. ``0.0`` emphasizes sparse\n            BM25 matching and ``1.0`` emphasizes dense vector matching.\n    \"\"\"\n\n    top_k: int = 4\n    similarity_threshold: float = 0.0\n    strategy: str = \"vector\"  # \"vector\", \"bm25\", \"hybrid\"\n    alpha: float = 0.5        # 0.0 = sparse (bm25), 1.0 = dense (vector)\n</code></pre>"},{"location":"reference/api/#hayagriva.config.WeaviateConfig","title":"<code>hayagriva.config.WeaviateConfig</code>  <code>dataclass</code>","text":"<p>Connection settings for the Weaviate vector store.</p> <p>Attributes:</p> Name Type Description <code>url</code> <code>str</code> <p>Base URL for the Weaviate instance.</p> <code>api_key</code> <code>Optional[str]</code> <p>Optional API key for authenticated hosted Weaviate instances.</p> <code>index_name</code> <code>str</code> <p>Weaviate class name used to store Hayagriva chunks.</p> Source code in <code>hayagriva/config.py</code> <pre><code>@dataclass\nclass WeaviateConfig:\n    \"\"\"Connection settings for the Weaviate vector store.\n\n    Attributes:\n        url: Base URL for the Weaviate instance.\n        api_key: Optional API key for authenticated hosted Weaviate instances.\n        index_name: Weaviate class name used to store Hayagriva chunks.\n    \"\"\"\n\n    url: str = \"http://localhost:8080\"\n    api_key: Optional[str] = None\n    index_name: str = \"HayagrivaDocs\"\n</code></pre>"},{"location":"reference/api/#chunking","title":"Chunking","text":""},{"location":"reference/api/#hayagriva.core.chunker.WordChunker","title":"<code>hayagriva.core.chunker.WordChunker</code>","text":"<p>               Bases: <code>BaseChunker</code></p> <p>Chunk text using a sliding whitespace word window.</p> Source code in <code>hayagriva/core/chunker.py</code> <pre><code>class WordChunker(BaseChunker):\n    \"\"\"Chunk text using a sliding whitespace word window.\"\"\"\n\n    def chunk(self, documents: Iterable[str]) -&gt; Tuple[List[str], List[dict]]:\n        \"\"\"Split documents into overlapping word chunks.\n\n        Args:\n            documents: Iterable of raw document strings.\n\n        Returns:\n            A tuple of chunk text and empty metadata dictionaries. Each chunk\n            contains up to ``config.chunk_size`` words and shares\n            ``config.overlap`` words with the previous chunk when possible.\n        \"\"\"\n        chunks: List[str] = []\n        metadata: List[dict] = []\n\n        for doc in documents:\n            words = doc.split()\n            size = self.config.chunk_size\n            overlap = self.config.overlap\n            start = 0\n            while start &lt; len(words):\n                end = start + size\n                chunk_words = words[start:end]\n                if chunk_words:\n                    chunks.append(\" \".join(chunk_words))\n                    metadata.append({})\n                start += size - overlap if size &gt; overlap else size\n        return chunks, metadata\n</code></pre>"},{"location":"reference/api/#hayagriva.core.chunker.WordChunker.chunk","title":"<code>chunk(documents)</code>","text":"<p>Split documents into overlapping word chunks.</p> <p>Parameters:</p> Name Type Description Default <code>documents</code> <code>Iterable[str]</code> <p>Iterable of raw document strings.</p> required <p>Returns:</p> Type Description <code>List[str]</code> <p>A tuple of chunk text and empty metadata dictionaries. Each chunk</p> <code>List[dict]</code> <p>contains up to <code>config.chunk_size</code> words and shares</p> <code>Tuple[List[str], List[dict]]</code> <p><code>config.overlap</code> words with the previous chunk when possible.</p> Source code in <code>hayagriva/core/chunker.py</code> <pre><code>def chunk(self, documents: Iterable[str]) -&gt; Tuple[List[str], List[dict]]:\n    \"\"\"Split documents into overlapping word chunks.\n\n    Args:\n        documents: Iterable of raw document strings.\n\n    Returns:\n        A tuple of chunk text and empty metadata dictionaries. Each chunk\n        contains up to ``config.chunk_size`` words and shares\n        ``config.overlap`` words with the previous chunk when possible.\n    \"\"\"\n    chunks: List[str] = []\n    metadata: List[dict] = []\n\n    for doc in documents:\n        words = doc.split()\n        size = self.config.chunk_size\n        overlap = self.config.overlap\n        start = 0\n        while start &lt; len(words):\n            end = start + size\n            chunk_words = words[start:end]\n            if chunk_words:\n                chunks.append(\" \".join(chunk_words))\n                metadata.append({})\n            start += size - overlap if size &gt; overlap else size\n    return chunks, metadata\n</code></pre>"},{"location":"reference/api/#hayagriva.core.chunker.RecursiveChunker","title":"<code>hayagriva.core.chunker.RecursiveChunker</code>","text":"<p>               Bases: <code>BaseChunker</code></p> <p>Chunk text recursively using an ordered separator list.</p> Source code in <code>hayagriva/core/chunker.py</code> <pre><code>class RecursiveChunker(BaseChunker):\n    \"\"\"Chunk text recursively using an ordered separator list.\"\"\"\n\n    def chunk(self, documents: Iterable[str]) -&gt; Tuple[List[str], List[dict]]:\n        \"\"\"Split documents by progressively smaller separators.\n\n        Args:\n            documents: Iterable of raw document strings.\n\n        Returns:\n            A tuple of chunk text and empty metadata dictionaries. The splitter\n            prefers larger separators first, then falls back to smaller\n            separators when a segment exceeds ``config.chunk_size``.\n        \"\"\"\n        chunks: List[str] = []\n        metadata: List[dict] = []\n\n        separators = self.config.separators\n\n        for doc in documents:\n            doc_chunks = self._recursive_split(doc, separators, self.config.chunk_size)\n            chunks.extend(doc_chunks)\n            metadata.extend([{} for _ in doc_chunks])\n\n        return chunks, metadata\n\n    def _recursive_split(self, text: str, separators: List[str], chunk_size: int) -&gt; List[str]:\n        \"\"\"Recursively split one document into chunk-sized segments.\n\n        Args:\n            text: Text segment to split.\n            separators: Ordered separators still available for splitting.\n            chunk_size: Target chunk size in approximate words.\n\n        Returns:\n            A list of text chunks.\n        \"\"\"\n        final_chunks = []\n\n        # Get the current separator\n        separator = separators[0]\n        new_separators = separators[1:]\n\n        # Split the text\n        if separator == \"\":\n            splits = list(text) # Character split\n        else:\n            splits = text.split(separator)\n\n        # Now combine splits into chunks\n        good_splits = []\n\n        for split in splits:\n            if not split.strip():\n                continue\n\n            # If split is small enough, keep it\n            # Note: We are approximating size by length of string / 5 (avg word length) + spaces\n            # Or just use character count? Config says chunk_size (int). \n            # WordChunker used words. Let's assume chunk_size is roughly words.\n            # 1 word ~ 5 chars.\n            if len(split.split()) &lt; chunk_size:\n                good_splits.append(split)\n            else:\n                # Split is too big, recurse\n                if new_separators:\n                    sub_chunks = self._recursive_split(split, new_separators, chunk_size)\n                    good_splits.extend(sub_chunks)\n                else:\n                    # No more separators, just take it (or force split)\n                    good_splits.append(split)\n\n        # Merge good splits into final chunks\n        current_chunk = []\n        current_len = 0\n\n        for split in good_splits:\n            split_len = len(split.split())\n            if current_len + split_len &gt; chunk_size:\n                if current_chunk:\n                    # Join with the separator we split by (approximation)\n                    # Actually, we lost the separator. Let's join with space or newline\n                    join_char = separator if separator != \"\" else \"\"\n                    if separator in [\"\\n\", \"\\n\\n\"]:\n                        join_char = separator\n                    else:\n                        join_char = \" \"\n\n                    final_chunks.append(join_char.join(current_chunk))\n\n                current_chunk = [split]\n                current_len = split_len\n            else:\n                current_chunk.append(split)\n                current_len += split_len\n\n        if current_chunk:\n            join_char = separator if separator in [\"\\n\", \"\\n\\n\"] else \" \"\n            final_chunks.append(join_char.join(current_chunk))\n\n        return final_chunks\n</code></pre>"},{"location":"reference/api/#hayagriva.core.chunker.RecursiveChunker.chunk","title":"<code>chunk(documents)</code>","text":"<p>Split documents by progressively smaller separators.</p> <p>Parameters:</p> Name Type Description Default <code>documents</code> <code>Iterable[str]</code> <p>Iterable of raw document strings.</p> required <p>Returns:</p> Type Description <code>List[str]</code> <p>A tuple of chunk text and empty metadata dictionaries. The splitter</p> <code>List[dict]</code> <p>prefers larger separators first, then falls back to smaller</p> <code>Tuple[List[str], List[dict]]</code> <p>separators when a segment exceeds <code>config.chunk_size</code>.</p> Source code in <code>hayagriva/core/chunker.py</code> <pre><code>def chunk(self, documents: Iterable[str]) -&gt; Tuple[List[str], List[dict]]:\n    \"\"\"Split documents by progressively smaller separators.\n\n    Args:\n        documents: Iterable of raw document strings.\n\n    Returns:\n        A tuple of chunk text and empty metadata dictionaries. The splitter\n        prefers larger separators first, then falls back to smaller\n        separators when a segment exceeds ``config.chunk_size``.\n    \"\"\"\n    chunks: List[str] = []\n    metadata: List[dict] = []\n\n    separators = self.config.separators\n\n    for doc in documents:\n        doc_chunks = self._recursive_split(doc, separators, self.config.chunk_size)\n        chunks.extend(doc_chunks)\n        metadata.extend([{} for _ in doc_chunks])\n\n    return chunks, metadata\n</code></pre>"},{"location":"reference/api/#hayagriva.core.chunker.SemanticChunker","title":"<code>hayagriva.core.chunker.SemanticChunker</code>","text":"<p>               Bases: <code>BaseChunker</code></p> <p>Chunk text based on semantic similarity between adjacent sentences.</p> Source code in <code>hayagriva/core/chunker.py</code> <pre><code>class SemanticChunker(BaseChunker):\n    \"\"\"Chunk text based on semantic similarity between adjacent sentences.\"\"\"\n\n    def __init__(self, config: ChunkingConfig | None = None, embedder: Optional[SentenceTransformerEmbeddings] = None) -&gt; None:\n        \"\"\"Initialize a semantic chunker.\n\n        Args:\n            config: Optional chunking configuration.\n            embedder: Embedder used to calculate sentence vectors. This is\n                required before ``chunk`` can run.\n        \"\"\"\n        super().__init__(config)\n        self.embedder = embedder\n\n    def chunk(self, documents: Iterable[str]) -&gt; Tuple[List[str], List[dict]]:\n        \"\"\"Split documents where adjacent sentence similarity drops.\n\n        Args:\n            documents: Iterable of raw document strings.\n\n        Returns:\n            A tuple of semantic chunks and empty metadata dictionaries.\n\n        Raises:\n            ValueError: If no embedder was provided.\n        \"\"\"\n        if not self.embedder:\n            raise ValueError(\"SemanticChunker requires an embedder.\")\n\n        import numpy as np\n\n        chunks: List[str] = []\n        metadata: List[dict] = []\n\n        for doc in documents:\n            # 1. Split into sentences (simple split)\n            sentences = re.split(r'(?&lt;=[.?!])\\s+', doc)\n            if not sentences:\n                continue\n\n            # 2. Embed sentences\n            embeddings = self.embedder.embed(sentences)\n\n            # 3. Calculate cosine similarity between adjacent sentences\n            # Norms are already handled by embedder? No, SentenceTransformer returns raw.\n            # But we can use dot product if normalized.\n            norms = np.linalg.norm(embeddings, axis=1, keepdims=True)\n            normalized = embeddings / norms\n\n            similarities = []\n            for i in range(len(normalized) - 1):\n                sim = np.dot(normalized[i], normalized[i+1])\n                similarities.append(sim)\n\n            # 4. Group sentences\n            current_chunk = [sentences[0]]\n\n            for i, sim in enumerate(similarities):\n                if sim &gt;= self.config.semantic_threshold:\n                    current_chunk.append(sentences[i+1])\n                else:\n                    chunks.append(\" \".join(current_chunk))\n                    metadata.append({})\n                    current_chunk = [sentences[i+1]]\n\n            if current_chunk:\n                chunks.append(\" \".join(current_chunk))\n                metadata.append({})\n\n        return chunks, metadata\n</code></pre>"},{"location":"reference/api/#hayagriva.core.chunker.SemanticChunker.__init__","title":"<code>__init__(config=None, embedder=None)</code>","text":"<p>Initialize a semantic chunker.</p> <p>Parameters:</p> Name Type Description Default <code>config</code> <code>ChunkingConfig | None</code> <p>Optional chunking configuration.</p> <code>None</code> <code>embedder</code> <code>Optional[SentenceTransformerEmbeddings]</code> <p>Embedder used to calculate sentence vectors. This is required before <code>chunk</code> can run.</p> <code>None</code> Source code in <code>hayagriva/core/chunker.py</code> <pre><code>def __init__(self, config: ChunkingConfig | None = None, embedder: Optional[SentenceTransformerEmbeddings] = None) -&gt; None:\n    \"\"\"Initialize a semantic chunker.\n\n    Args:\n        config: Optional chunking configuration.\n        embedder: Embedder used to calculate sentence vectors. This is\n            required before ``chunk`` can run.\n    \"\"\"\n    super().__init__(config)\n    self.embedder = embedder\n</code></pre>"},{"location":"reference/api/#hayagriva.core.chunker.SemanticChunker.chunk","title":"<code>chunk(documents)</code>","text":"<p>Split documents where adjacent sentence similarity drops.</p> <p>Parameters:</p> Name Type Description Default <code>documents</code> <code>Iterable[str]</code> <p>Iterable of raw document strings.</p> required <p>Returns:</p> Type Description <code>Tuple[List[str], List[dict]]</code> <p>A tuple of semantic chunks and empty metadata dictionaries.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If no embedder was provided.</p> Source code in <code>hayagriva/core/chunker.py</code> <pre><code>def chunk(self, documents: Iterable[str]) -&gt; Tuple[List[str], List[dict]]:\n    \"\"\"Split documents where adjacent sentence similarity drops.\n\n    Args:\n        documents: Iterable of raw document strings.\n\n    Returns:\n        A tuple of semantic chunks and empty metadata dictionaries.\n\n    Raises:\n        ValueError: If no embedder was provided.\n    \"\"\"\n    if not self.embedder:\n        raise ValueError(\"SemanticChunker requires an embedder.\")\n\n    import numpy as np\n\n    chunks: List[str] = []\n    metadata: List[dict] = []\n\n    for doc in documents:\n        # 1. Split into sentences (simple split)\n        sentences = re.split(r'(?&lt;=[.?!])\\s+', doc)\n        if not sentences:\n            continue\n\n        # 2. Embed sentences\n        embeddings = self.embedder.embed(sentences)\n\n        # 3. Calculate cosine similarity between adjacent sentences\n        # Norms are already handled by embedder? No, SentenceTransformer returns raw.\n        # But we can use dot product if normalized.\n        norms = np.linalg.norm(embeddings, axis=1, keepdims=True)\n        normalized = embeddings / norms\n\n        similarities = []\n        for i in range(len(normalized) - 1):\n            sim = np.dot(normalized[i], normalized[i+1])\n            similarities.append(sim)\n\n        # 4. Group sentences\n        current_chunk = [sentences[0]]\n\n        for i, sim in enumerate(similarities):\n            if sim &gt;= self.config.semantic_threshold:\n                current_chunk.append(sentences[i+1])\n            else:\n                chunks.append(\" \".join(current_chunk))\n                metadata.append({})\n                current_chunk = [sentences[i+1]]\n\n        if current_chunk:\n            chunks.append(\" \".join(current_chunk))\n            metadata.append({})\n\n    return chunks, metadata\n</code></pre>"},{"location":"reference/api/#hayagriva.core.chunker.HierarchicalChunker","title":"<code>hayagriva.core.chunker.HierarchicalChunker</code>","text":"<p>               Bases: <code>BaseChunker</code></p> <p>Create child chunks while preserving larger parent chunk context.</p> Source code in <code>hayagriva/core/chunker.py</code> <pre><code>class HierarchicalChunker(BaseChunker):\n    \"\"\"Create child chunks while preserving larger parent chunk context.\"\"\"\n\n    def chunk(self, documents: Iterable[str]) -&gt; Tuple[List[str], List[dict]]:\n        \"\"\"Split documents into parent chunks and then child chunks.\n\n        Args:\n            documents: Iterable of raw document strings.\n\n        Returns:\n            A tuple ``(child_chunks, metadata)``. Each metadata record contains\n            ``parent_text`` with the larger source chunk that produced the\n            returned child chunk.\n        \"\"\"\n        chunks: List[str] = []\n        metadata: List[dict] = []\n\n        # Use WordChunker logic for simplicity\n        word_chunker = WordChunker(self.config)\n\n        for doc in documents:\n            # 1. Create Parent Chunks\n            # Temporarily override config for parent size\n            original_size = self.config.chunk_size\n            self.config.chunk_size = self.config.parent_chunk_size\n\n            parent_chunks, _ = word_chunker.chunk([doc])\n\n            # Restore config\n            self.config.chunk_size = original_size\n\n            # 2. Create Child Chunks from Parent Chunks\n            for parent in parent_chunks:\n                child_chunks, _ = word_chunker.chunk([parent])\n\n                for child in child_chunks:\n                    chunks.append(child)\n                    metadata.append({\"parent_text\": parent})\n\n        return chunks, metadata\n</code></pre>"},{"location":"reference/api/#hayagriva.core.chunker.HierarchicalChunker.chunk","title":"<code>chunk(documents)</code>","text":"<p>Split documents into parent chunks and then child chunks.</p> <p>Parameters:</p> Name Type Description Default <code>documents</code> <code>Iterable[str]</code> <p>Iterable of raw document strings.</p> required <p>Returns:</p> Type Description <code>List[str]</code> <p>A tuple <code>(child_chunks, metadata)</code>. Each metadata record contains</p> <code>List[dict]</code> <p><code>parent_text</code> with the larger source chunk that produced the</p> <code>Tuple[List[str], List[dict]]</code> <p>returned child chunk.</p> Source code in <code>hayagriva/core/chunker.py</code> <pre><code>def chunk(self, documents: Iterable[str]) -&gt; Tuple[List[str], List[dict]]:\n    \"\"\"Split documents into parent chunks and then child chunks.\n\n    Args:\n        documents: Iterable of raw document strings.\n\n    Returns:\n        A tuple ``(child_chunks, metadata)``. Each metadata record contains\n        ``parent_text`` with the larger source chunk that produced the\n        returned child chunk.\n    \"\"\"\n    chunks: List[str] = []\n    metadata: List[dict] = []\n\n    # Use WordChunker logic for simplicity\n    word_chunker = WordChunker(self.config)\n\n    for doc in documents:\n        # 1. Create Parent Chunks\n        # Temporarily override config for parent size\n        original_size = self.config.chunk_size\n        self.config.chunk_size = self.config.parent_chunk_size\n\n        parent_chunks, _ = word_chunker.chunk([doc])\n\n        # Restore config\n        self.config.chunk_size = original_size\n\n        # 2. Create Child Chunks from Parent Chunks\n        for parent in parent_chunks:\n            child_chunks, _ = word_chunker.chunk([parent])\n\n            for child in child_chunks:\n                chunks.append(child)\n                metadata.append({\"parent_text\": parent})\n\n    return chunks, metadata\n</code></pre>"},{"location":"reference/api/#embeddings","title":"Embeddings","text":""},{"location":"reference/api/#hayagriva.core.embeddings.SentenceTransformerEmbeddings","title":"<code>hayagriva.core.embeddings.SentenceTransformerEmbeddings</code>","text":"<p>Sentence-Transformers embedding wrapper.</p> <p>Parameters:</p> Name Type Description Default <code>model_name</code> <code>str</code> <p>Hugging Face or Sentence-Transformers model identifier.</p> <code>'all-MiniLM-L6-v2'</code> <code>device</code> <code>Optional[str]</code> <p>Optional device passed to <code>SentenceTransformer</code> such as <code>\"cpu\"</code> or <code>\"cuda\"</code>.</p> <code>None</code> <p>Attributes:</p> Name Type Description <code>model</code> <p>Loaded <code>SentenceTransformer</code> model instance.</p> <code>model_name</code> <p>Name of the embedding model in use.</p> <p>Raises:</p> Type Description <code>MissingDependencyError</code> <p>If <code>sentence-transformers</code> is not installed.</p> Source code in <code>hayagriva/core/embeddings.py</code> <pre><code>class SentenceTransformerEmbeddings:\n    \"\"\"Sentence-Transformers embedding wrapper.\n\n    Args:\n        model_name: Hugging Face or Sentence-Transformers model identifier.\n        device: Optional device passed to ``SentenceTransformer`` such as\n            ``\"cpu\"`` or ``\"cuda\"``.\n\n    Attributes:\n        model: Loaded ``SentenceTransformer`` model instance.\n        model_name: Name of the embedding model in use.\n\n    Raises:\n        MissingDependencyError: If ``sentence-transformers`` is not installed.\n    \"\"\"\n\n    def __init__(self, model_name: str = \"all-MiniLM-L6-v2\", device: Optional[str] = None) -&gt; None:\n        \"\"\"Load a Sentence-Transformers model.\n\n        Args:\n            model_name: Model identifier to load.\n            device: Optional execution device.\n\n        Raises:\n            MissingDependencyError: If ``sentence-transformers`` is missing.\n        \"\"\"\n        if importlib.util.find_spec(\"sentence_transformers\") is None:\n            raise MissingDependencyError(\n                \"sentence-transformers is required for embedding. Install with `pip install sentence-transformers`.\"\n            )\n        from sentence_transformers import SentenceTransformer\n\n        self.model = SentenceTransformer(model_name, device=device)\n        self.model_name = model_name\n\n    def embed(self, texts: Iterable[str]) -&gt; np.ndarray:\n        \"\"\"Embed a batch of texts.\n\n        Args:\n            texts: Iterable of text strings to encode.\n\n        Returns:\n            NumPy array with shape ``(len(texts), embedding_dimension)``.\n\n        Raises:\n            MissingDependencyError: If NumPy is not installed.\n        \"\"\"\n        text_list = list(texts)\n        logger.info(\"Encoding %d texts with %s\", len(text_list), self.model_name)\n        if importlib.util.find_spec(\"numpy\") is None:\n            raise MissingDependencyError(\"numpy is required for embedding operations.\")\n        import numpy as np\n\n        return np.array(self.model.encode(text_list, convert_to_numpy=True))\n\n    def embed_query(self, text: str) -&gt; np.ndarray:\n        \"\"\"Embed a single query string.\n\n        Args:\n            text: Query text to encode.\n\n        Returns:\n            One-dimensional NumPy vector for the query.\n\n        Raises:\n            MissingDependencyError: If NumPy is not installed.\n        \"\"\"\n        if importlib.util.find_spec(\"numpy\") is None:\n            raise MissingDependencyError(\"numpy is required for embedding operations.\")\n        import numpy as np\n\n        return np.array(self.model.encode([text], convert_to_numpy=True))[0]\n</code></pre>"},{"location":"reference/api/#hayagriva.core.embeddings.SentenceTransformerEmbeddings.__init__","title":"<code>__init__(model_name='all-MiniLM-L6-v2', device=None)</code>","text":"<p>Load a Sentence-Transformers model.</p> <p>Parameters:</p> Name Type Description Default <code>model_name</code> <code>str</code> <p>Model identifier to load.</p> <code>'all-MiniLM-L6-v2'</code> <code>device</code> <code>Optional[str]</code> <p>Optional execution device.</p> <code>None</code> <p>Raises:</p> Type Description <code>MissingDependencyError</code> <p>If <code>sentence-transformers</code> is missing.</p> Source code in <code>hayagriva/core/embeddings.py</code> <pre><code>def __init__(self, model_name: str = \"all-MiniLM-L6-v2\", device: Optional[str] = None) -&gt; None:\n    \"\"\"Load a Sentence-Transformers model.\n\n    Args:\n        model_name: Model identifier to load.\n        device: Optional execution device.\n\n    Raises:\n        MissingDependencyError: If ``sentence-transformers`` is missing.\n    \"\"\"\n    if importlib.util.find_spec(\"sentence_transformers\") is None:\n        raise MissingDependencyError(\n            \"sentence-transformers is required for embedding. Install with `pip install sentence-transformers`.\"\n        )\n    from sentence_transformers import SentenceTransformer\n\n    self.model = SentenceTransformer(model_name, device=device)\n    self.model_name = model_name\n</code></pre>"},{"location":"reference/api/#hayagriva.core.embeddings.SentenceTransformerEmbeddings.embed","title":"<code>embed(texts)</code>","text":"<p>Embed a batch of texts.</p> <p>Parameters:</p> Name Type Description Default <code>texts</code> <code>Iterable[str]</code> <p>Iterable of text strings to encode.</p> required <p>Returns:</p> Type Description <code>ndarray</code> <p>NumPy array with shape <code>(len(texts), embedding_dimension)</code>.</p> <p>Raises:</p> Type Description <code>MissingDependencyError</code> <p>If NumPy is not installed.</p> Source code in <code>hayagriva/core/embeddings.py</code> <pre><code>def embed(self, texts: Iterable[str]) -&gt; np.ndarray:\n    \"\"\"Embed a batch of texts.\n\n    Args:\n        texts: Iterable of text strings to encode.\n\n    Returns:\n        NumPy array with shape ``(len(texts), embedding_dimension)``.\n\n    Raises:\n        MissingDependencyError: If NumPy is not installed.\n    \"\"\"\n    text_list = list(texts)\n    logger.info(\"Encoding %d texts with %s\", len(text_list), self.model_name)\n    if importlib.util.find_spec(\"numpy\") is None:\n        raise MissingDependencyError(\"numpy is required for embedding operations.\")\n    import numpy as np\n\n    return np.array(self.model.encode(text_list, convert_to_numpy=True))\n</code></pre>"},{"location":"reference/api/#hayagriva.core.embeddings.SentenceTransformerEmbeddings.embed_query","title":"<code>embed_query(text)</code>","text":"<p>Embed a single query string.</p> <p>Parameters:</p> Name Type Description Default <code>text</code> <code>str</code> <p>Query text to encode.</p> required <p>Returns:</p> Type Description <code>ndarray</code> <p>One-dimensional NumPy vector for the query.</p> <p>Raises:</p> Type Description <code>MissingDependencyError</code> <p>If NumPy is not installed.</p> Source code in <code>hayagriva/core/embeddings.py</code> <pre><code>def embed_query(self, text: str) -&gt; np.ndarray:\n    \"\"\"Embed a single query string.\n\n    Args:\n        text: Query text to encode.\n\n    Returns:\n        One-dimensional NumPy vector for the query.\n\n    Raises:\n        MissingDependencyError: If NumPy is not installed.\n    \"\"\"\n    if importlib.util.find_spec(\"numpy\") is None:\n        raise MissingDependencyError(\"numpy is required for embedding operations.\")\n    import numpy as np\n\n    return np.array(self.model.encode([text], convert_to_numpy=True))[0]\n</code></pre>"},{"location":"reference/api/#retrieval","title":"Retrieval","text":""},{"location":"reference/api/#hayagriva.core.retriever.Retriever","title":"<code>hayagriva.core.retriever.Retriever</code>","text":"<p>Coordinate query embedding and vector store search.</p> <p>Parameters:</p> Name Type Description Default <code>embedder</code> <code>SentenceTransformerEmbeddings</code> <p>Embedding provider used to encode chunks and queries.</p> required <code>vector_store</code> <code>Any</code> <p>Vector store object implementing <code>add</code> and <code>search</code>.</p> required <code>config</code> <code>RetrievalConfig | None</code> <p>Optional retrieval configuration.</p> <code>None</code> <p>Attributes:</p> Name Type Description <code>embedder</code> <p>Effective embedding provider.</p> <code>vector_store</code> <p>Effective vector store backend.</p> <code>config</code> <p>Effective retrieval configuration.</p> Source code in <code>hayagriva/core/retriever.py</code> <pre><code>class Retriever:\n    \"\"\"Coordinate query embedding and vector store search.\n\n    Args:\n        embedder: Embedding provider used to encode chunks and queries.\n        vector_store: Vector store object implementing ``add`` and ``search``.\n        config: Optional retrieval configuration.\n\n    Attributes:\n        embedder: Effective embedding provider.\n        vector_store: Effective vector store backend.\n        config: Effective retrieval configuration.\n    \"\"\"\n\n    def __init__(\n        self,\n        embedder: SentenceTransformerEmbeddings,\n        vector_store: Any,\n        config: RetrievalConfig | None = None,\n    ) -&gt; None:\n        \"\"\"Initialize a retriever with an embedder and vector store.\n\n        Args:\n            embedder: Embedding provider used for chunks and queries.\n            vector_store: Vector store backend implementing ``add`` and\n                ``search``.\n            config: Optional retrieval configuration.\n        \"\"\"\n        self.embedder = embedder\n        self.vector_store = vector_store\n        self.config = config or RetrievalConfig()\n\n    def add(self, chunks: Iterable[str], metadata: Iterable[dict] | None = None) -&gt; None:\n        \"\"\"Embed chunks and add them to the configured vector store.\n\n        Args:\n            chunks: Iterable of chunk strings to index.\n            metadata: Optional iterable of metadata dictionaries aligned with\n                ``chunks``.\n        \"\"\"\n        chunk_list = list(chunks)\n        embeddings = self.embedder.embed(chunk_list)\n\n        meta_list = list(metadata) if metadata else None\n        self.vector_store.add(embeddings, chunk_list, metadata=meta_list)\n\n        logger.info(\"Added %d chunks to vector store\", len(chunk_list))\n\n    def retrieve(self, query: str) -&gt; List[Tuple[str, float]]:\n        \"\"\"Retrieve chunks relevant to a query.\n\n        Args:\n            query: Natural-language search query.\n\n        Returns:\n            List of ``(chunk_text, score)`` pairs after applying the configured\n            ``top_k`` and optional similarity threshold.\n\n        Raises:\n            ConfigurationError: If ``top_k`` is invalid.\n            ValueError: If the vector store rejects the configured retrieval\n                strategy.\n        \"\"\"\n        top_k = validate_top_k(self.config.top_k)\n        query_embedding = self.embedder.embed_query(query)\n\n        results = self.vector_store.search(\n            query_embedding, \n            top_k=top_k, \n            query_text=query,\n            strategy=self.config.strategy,\n            alpha=self.config.alpha\n        )\n\n        if self.config.similarity_threshold &gt; 0:\n            results = [r for r in results if r[1] &gt;= self.config.similarity_threshold]\n        logger.info(\"Retrieved %d chunks\", len(results))\n        return results\n</code></pre>"},{"location":"reference/api/#hayagriva.core.retriever.Retriever.__init__","title":"<code>__init__(embedder, vector_store, config=None)</code>","text":"<p>Initialize a retriever with an embedder and vector store.</p> <p>Parameters:</p> Name Type Description Default <code>embedder</code> <code>SentenceTransformerEmbeddings</code> <p>Embedding provider used for chunks and queries.</p> required <code>vector_store</code> <code>Any</code> <p>Vector store backend implementing <code>add</code> and <code>search</code>.</p> required <code>config</code> <code>RetrievalConfig | None</code> <p>Optional retrieval configuration.</p> <code>None</code> Source code in <code>hayagriva/core/retriever.py</code> <pre><code>def __init__(\n    self,\n    embedder: SentenceTransformerEmbeddings,\n    vector_store: Any,\n    config: RetrievalConfig | None = None,\n) -&gt; None:\n    \"\"\"Initialize a retriever with an embedder and vector store.\n\n    Args:\n        embedder: Embedding provider used for chunks and queries.\n        vector_store: Vector store backend implementing ``add`` and\n            ``search``.\n        config: Optional retrieval configuration.\n    \"\"\"\n    self.embedder = embedder\n    self.vector_store = vector_store\n    self.config = config or RetrievalConfig()\n</code></pre>"},{"location":"reference/api/#hayagriva.core.retriever.Retriever.add","title":"<code>add(chunks, metadata=None)</code>","text":"<p>Embed chunks and add them to the configured vector store.</p> <p>Parameters:</p> Name Type Description Default <code>chunks</code> <code>Iterable[str]</code> <p>Iterable of chunk strings to index.</p> required <code>metadata</code> <code>Iterable[dict] | None</code> <p>Optional iterable of metadata dictionaries aligned with <code>chunks</code>.</p> <code>None</code> Source code in <code>hayagriva/core/retriever.py</code> <pre><code>def add(self, chunks: Iterable[str], metadata: Iterable[dict] | None = None) -&gt; None:\n    \"\"\"Embed chunks and add them to the configured vector store.\n\n    Args:\n        chunks: Iterable of chunk strings to index.\n        metadata: Optional iterable of metadata dictionaries aligned with\n            ``chunks``.\n    \"\"\"\n    chunk_list = list(chunks)\n    embeddings = self.embedder.embed(chunk_list)\n\n    meta_list = list(metadata) if metadata else None\n    self.vector_store.add(embeddings, chunk_list, metadata=meta_list)\n\n    logger.info(\"Added %d chunks to vector store\", len(chunk_list))\n</code></pre>"},{"location":"reference/api/#hayagriva.core.retriever.Retriever.retrieve","title":"<code>retrieve(query)</code>","text":"<p>Retrieve chunks relevant to a query.</p> <p>Parameters:</p> Name Type Description Default <code>query</code> <code>str</code> <p>Natural-language search query.</p> required <p>Returns:</p> Type Description <code>List[Tuple[str, float]]</code> <p>List of <code>(chunk_text, score)</code> pairs after applying the configured</p> <code>List[Tuple[str, float]]</code> <p><code>top_k</code> and optional similarity threshold.</p> <p>Raises:</p> Type Description <code>ConfigurationError</code> <p>If <code>top_k</code> is invalid.</p> <code>ValueError</code> <p>If the vector store rejects the configured retrieval strategy.</p> Source code in <code>hayagriva/core/retriever.py</code> <pre><code>def retrieve(self, query: str) -&gt; List[Tuple[str, float]]:\n    \"\"\"Retrieve chunks relevant to a query.\n\n    Args:\n        query: Natural-language search query.\n\n    Returns:\n        List of ``(chunk_text, score)`` pairs after applying the configured\n        ``top_k`` and optional similarity threshold.\n\n    Raises:\n        ConfigurationError: If ``top_k`` is invalid.\n        ValueError: If the vector store rejects the configured retrieval\n            strategy.\n    \"\"\"\n    top_k = validate_top_k(self.config.top_k)\n    query_embedding = self.embedder.embed_query(query)\n\n    results = self.vector_store.search(\n        query_embedding, \n        top_k=top_k, \n        query_text=query,\n        strategy=self.config.strategy,\n        alpha=self.config.alpha\n    )\n\n    if self.config.similarity_threshold &gt; 0:\n        results = [r for r in results if r[1] &gt;= self.config.similarity_threshold]\n    logger.info(\"Retrieved %d chunks\", len(results))\n    return results\n</code></pre>"},{"location":"reference/api/#vector-stores","title":"Vector Stores","text":""},{"location":"reference/api/#hayagriva.core.vectorstores.FaissVectorStore","title":"<code>hayagriva.core.vectorstores.FaissVectorStore</code>","text":"<p>A lightweight in-memory FAISS vector store.</p> <p>The store normalizes vectors and uses FAISS <code>IndexFlatIP</code> so scores are cosine-like inner products for normalized embeddings.</p> <p>Attributes:</p> Name Type Description <code>faiss</code> <p>Imported FAISS module.</p> <code>index</code> <code>IndexFlatIP | None</code> <p>FAISS index, initialized lazily on first <code>add</code>.</p> <code>vectors</code> <code>List</code> <p>Normalized vectors added to the store.</p> <code>chunks</code> <code>List[str]</code> <p>Chunk text aligned with stored vectors.</p> <code>metadata</code> <code>List[dict]</code> <p>Metadata dictionaries aligned with stored vectors.</p> <p>Raises:</p> Type Description <code>MissingDependencyError</code> <p>If <code>faiss-cpu</code> or <code>faiss-gpu</code> is not installed.</p> Source code in <code>hayagriva/core/vectorstores/faiss.py</code> <pre><code>class FaissVectorStore:\n    \"\"\"A lightweight in-memory FAISS vector store.\n\n    The store normalizes vectors and uses FAISS ``IndexFlatIP`` so scores are\n    cosine-like inner products for normalized embeddings.\n\n    Attributes:\n        faiss: Imported FAISS module.\n        index: FAISS index, initialized lazily on first ``add``.\n        vectors: Normalized vectors added to the store.\n        chunks: Chunk text aligned with stored vectors.\n        metadata: Metadata dictionaries aligned with stored vectors.\n\n    Raises:\n        MissingDependencyError: If ``faiss-cpu`` or ``faiss-gpu`` is not\n            installed.\n    \"\"\"\n\n    def __init__(self) -&gt; None:\n        \"\"\"Initialize an empty FAISS index wrapper.\n\n        The underlying FAISS index is created lazily when vectors are first\n        added, because the embedding dimension is not known before then.\n\n        Raises:\n            MissingDependencyError: If FAISS is not installed.\n        \"\"\"\n        if importlib.util.find_spec(\"faiss\") is None:\n            raise MissingDependencyError(\n                \"faiss-cpu is required for the FAISS vector store. Install with `pip install faiss-cpu`.\"\n            )\n        import faiss  # type: ignore\n\n        self.faiss = faiss\n        self.index: faiss.IndexFlatIP | None = None\n        self.vectors: List = []\n        self.chunks: List[str] = []\n        self.metadata: List[dict] = []\n\n    def add(self, embeddings: Any, chunks: Sequence[str], metadata: Sequence[dict] | None = None) -&gt; None:\n        \"\"\"Add embedded chunks to the FAISS index.\n\n        Args:\n            embeddings: Two-dimensional NumPy array of chunk embeddings.\n            chunks: Chunk text aligned with ``embeddings``.\n            metadata: Optional metadata dictionaries aligned with ``chunks``.\n\n        Raises:\n            ValueError: If embeddings, chunks, and metadata lengths do not\n                match.\n        \"\"\"\n        if len(embeddings) != len(chunks):\n            raise ValueError(\"Embeddings and chunks length mismatch\")\n        if embeddings.size == 0:\n            return\n        if self.index is None:\n            dimension = embeddings.shape[1]\n            self.index = self.faiss.IndexFlatIP(dimension)\n            logger.info(\"Initialized FAISS index with dimension %d\", dimension)\n        normalized = self._normalize(embeddings)\n        self.index.add(normalized)\n        self.vectors.extend(normalized)\n        self.chunks.extend(list(chunks))\n\n        if metadata is None:\n            self.metadata.extend([{} for _ in chunks])\n        else:\n            if len(metadata) != len(chunks):\n                raise ValueError(\"Metadata length mismatch\")\n            self.metadata.extend(list(metadata))\n\n    def search(\n        self,\n        query_embedding: Any,\n        top_k: int = 4,\n        query_text: str = \"\",\n        **kwargs: Any,\n    ) -&gt; List[Tuple[str, float]]:\n        \"\"\"Search the index for chunks nearest to a query vector.\n\n        Args:\n            query_embedding: One-dimensional query embedding.\n            top_k: Maximum number of chunks to return.\n            query_text: Unused text query argument accepted for retriever\n                compatibility.\n            **kwargs: Additional unused search options accepted for\n                compatibility with other vector stores.\n\n        Returns:\n            List of ``(chunk_text, score)`` pairs sorted by FAISS score.\n        \"\"\"\n        if self.index is None or len(self.chunks) == 0:\n            return []\n        normalized = self._normalize(query_embedding.reshape(1, -1))\n        scores, indices = self.index.search(normalized, top_k)\n        results: List[Tuple[str, float]] = []\n        for score_row, idx_row in zip(scores, indices):\n            for score, idx in zip(score_row, idx_row):\n                if idx == -1:\n                    continue\n                results.append((self.chunks[idx], float(score)))\n        return results\n\n    def _normalize(self, vectors: Any) -&gt; Any:\n        \"\"\"Normalize vectors row-wise for cosine-style inner-product search.\n\n        Args:\n            vectors: Two-dimensional NumPy array.\n\n        Returns:\n            Row-normalized NumPy array.\n\n        Raises:\n            MissingDependencyError: If NumPy is not installed.\n        \"\"\"\n        if importlib.util.find_spec(\"numpy\") is None:\n            raise MissingDependencyError(\"numpy is required for vector operations.\")\n        import numpy as np\n\n        norms = np.linalg.norm(vectors, axis=1, keepdims=True)\n        norms[norms == 0] = 1.0\n        return vectors / norms\n</code></pre>"},{"location":"reference/api/#hayagriva.core.vectorstores.FaissVectorStore.__init__","title":"<code>__init__()</code>","text":"<p>Initialize an empty FAISS index wrapper.</p> <p>The underlying FAISS index is created lazily when vectors are first added, because the embedding dimension is not known before then.</p> <p>Raises:</p> Type Description <code>MissingDependencyError</code> <p>If FAISS is not installed.</p> Source code in <code>hayagriva/core/vectorstores/faiss.py</code> <pre><code>def __init__(self) -&gt; None:\n    \"\"\"Initialize an empty FAISS index wrapper.\n\n    The underlying FAISS index is created lazily when vectors are first\n    added, because the embedding dimension is not known before then.\n\n    Raises:\n        MissingDependencyError: If FAISS is not installed.\n    \"\"\"\n    if importlib.util.find_spec(\"faiss\") is None:\n        raise MissingDependencyError(\n            \"faiss-cpu is required for the FAISS vector store. Install with `pip install faiss-cpu`.\"\n        )\n    import faiss  # type: ignore\n\n    self.faiss = faiss\n    self.index: faiss.IndexFlatIP | None = None\n    self.vectors: List = []\n    self.chunks: List[str] = []\n    self.metadata: List[dict] = []\n</code></pre>"},{"location":"reference/api/#hayagriva.core.vectorstores.FaissVectorStore.add","title":"<code>add(embeddings, chunks, metadata=None)</code>","text":"<p>Add embedded chunks to the FAISS index.</p> <p>Parameters:</p> Name Type Description Default <code>embeddings</code> <code>Any</code> <p>Two-dimensional NumPy array of chunk embeddings.</p> required <code>chunks</code> <code>Sequence[str]</code> <p>Chunk text aligned with <code>embeddings</code>.</p> required <code>metadata</code> <code>Sequence[dict] | None</code> <p>Optional metadata dictionaries aligned with <code>chunks</code>.</p> <code>None</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>If embeddings, chunks, and metadata lengths do not match.</p> Source code in <code>hayagriva/core/vectorstores/faiss.py</code> <pre><code>def add(self, embeddings: Any, chunks: Sequence[str], metadata: Sequence[dict] | None = None) -&gt; None:\n    \"\"\"Add embedded chunks to the FAISS index.\n\n    Args:\n        embeddings: Two-dimensional NumPy array of chunk embeddings.\n        chunks: Chunk text aligned with ``embeddings``.\n        metadata: Optional metadata dictionaries aligned with ``chunks``.\n\n    Raises:\n        ValueError: If embeddings, chunks, and metadata lengths do not\n            match.\n    \"\"\"\n    if len(embeddings) != len(chunks):\n        raise ValueError(\"Embeddings and chunks length mismatch\")\n    if embeddings.size == 0:\n        return\n    if self.index is None:\n        dimension = embeddings.shape[1]\n        self.index = self.faiss.IndexFlatIP(dimension)\n        logger.info(\"Initialized FAISS index with dimension %d\", dimension)\n    normalized = self._normalize(embeddings)\n    self.index.add(normalized)\n    self.vectors.extend(normalized)\n    self.chunks.extend(list(chunks))\n\n    if metadata is None:\n        self.metadata.extend([{} for _ in chunks])\n    else:\n        if len(metadata) != len(chunks):\n            raise ValueError(\"Metadata length mismatch\")\n        self.metadata.extend(list(metadata))\n</code></pre>"},{"location":"reference/api/#hayagriva.core.vectorstores.FaissVectorStore.search","title":"<code>search(query_embedding, top_k=4, query_text='', **kwargs)</code>","text":"<p>Search the index for chunks nearest to a query vector.</p> <p>Parameters:</p> Name Type Description Default <code>query_embedding</code> <code>Any</code> <p>One-dimensional query embedding.</p> required <code>top_k</code> <code>int</code> <p>Maximum number of chunks to return.</p> <code>4</code> <code>query_text</code> <code>str</code> <p>Unused text query argument accepted for retriever compatibility.</p> <code>''</code> <code>**kwargs</code> <code>Any</code> <p>Additional unused search options accepted for compatibility with other vector stores.</p> <code>{}</code> <p>Returns:</p> Type Description <code>List[Tuple[str, float]]</code> <p>List of <code>(chunk_text, score)</code> pairs sorted by FAISS score.</p> Source code in <code>hayagriva/core/vectorstores/faiss.py</code> <pre><code>def search(\n    self,\n    query_embedding: Any,\n    top_k: int = 4,\n    query_text: str = \"\",\n    **kwargs: Any,\n) -&gt; List[Tuple[str, float]]:\n    \"\"\"Search the index for chunks nearest to a query vector.\n\n    Args:\n        query_embedding: One-dimensional query embedding.\n        top_k: Maximum number of chunks to return.\n        query_text: Unused text query argument accepted for retriever\n            compatibility.\n        **kwargs: Additional unused search options accepted for\n            compatibility with other vector stores.\n\n    Returns:\n        List of ``(chunk_text, score)`` pairs sorted by FAISS score.\n    \"\"\"\n    if self.index is None or len(self.chunks) == 0:\n        return []\n    normalized = self._normalize(query_embedding.reshape(1, -1))\n    scores, indices = self.index.search(normalized, top_k)\n    results: List[Tuple[str, float]] = []\n    for score_row, idx_row in zip(scores, indices):\n        for score, idx in zip(score_row, idx_row):\n            if idx == -1:\n                continue\n            results.append((self.chunks[idx], float(score)))\n    return results\n</code></pre>"},{"location":"reference/api/#hayagriva.core.vectorstores.WeaviateVectorStore","title":"<code>hayagriva.core.vectorstores.WeaviateVectorStore</code>","text":"<p>Weaviate vector store wrapper.</p> <p>Parameters:</p> Name Type Description Default <code>config</code> <code>WeaviateConfig</code> <p>Weaviate connection and class configuration.</p> required <p>Attributes:</p> Name Type Description <code>config</code> <p>Effective Weaviate configuration.</p> <code>client</code> <p>Weaviate v3 client instance.</p> <p>Raises:</p> Type Description <code>MissingDependencyError</code> <p>If <code>weaviate-client</code> is not installed.</p> Source code in <code>hayagriva/core/vectorstores/weaviate.py</code> <pre><code>class WeaviateVectorStore:\n    \"\"\"Weaviate vector store wrapper.\n\n    Args:\n        config: Weaviate connection and class configuration.\n\n    Attributes:\n        config: Effective Weaviate configuration.\n        client: Weaviate v3 client instance.\n\n    Raises:\n        MissingDependencyError: If ``weaviate-client`` is not installed.\n    \"\"\"\n\n    def __init__(self, config: WeaviateConfig) -&gt; None:\n        \"\"\"Connect to Weaviate and ensure the target class exists.\n\n        Args:\n            config: Weaviate connection and class configuration.\n\n        Raises:\n            MissingDependencyError: If ``weaviate-client`` is not installed.\n        \"\"\"\n        if importlib.util.find_spec(\"weaviate\") is None:\n            raise MissingDependencyError(\n                \"weaviate-client is required for Weaviate support. Install with `pip install weaviate-client`.\"\n            )\n        import weaviate\n\n        self.config = config\n        self.client = weaviate.Client(\n            url=config.url,\n            auth_client_secret=(\n                weaviate.auth.AuthApiKey(api_key=config.api_key) if config.api_key else None\n            ),\n        )\n\n        self._ensure_schema()\n\n    def _ensure_schema(self) -&gt; None:\n        \"\"\"Create the configured Weaviate class if it does not exist.\n\n        The class disables Weaviate-side vectorization because Hayagriva\n        supplies vectors directly.\n        \"\"\"\n        class_obj = {\n            \"class\": self.config.index_name,\n            \"vectorizer\": \"none\",\n            \"properties\": [\n                {\n                    \"name\": \"text\",\n                    \"dataType\": [\"text\"],\n                },\n                {\n                    \"name\": \"parent_text\",\n                    \"dataType\": [\"text\"],\n                },\n            ],\n        }\n\n        if not self.client.schema.exists(self.config.index_name):\n            self.client.schema.create_class(class_obj)\n            logger.info(\"Created Weaviate class: %s\", self.config.index_name)\n\n    def add(self, embeddings: Any, chunks: Sequence[str], metadata: Sequence[dict] | None = None) -&gt; None:\n        \"\"\"Add embedded chunks to Weaviate.\n\n        Args:\n            embeddings: Two-dimensional array-like object of chunk embeddings.\n            chunks: Chunk text aligned with ``embeddings``.\n            metadata: Optional metadata dictionaries. ``parent_text`` is stored\n                when present.\n\n        Raises:\n            ValueError: If embeddings and chunks have different lengths.\n        \"\"\"\n        if len(embeddings) != len(chunks):\n            raise ValueError(\"Embeddings and chunks length mismatch\")\n\n        if len(chunks) == 0:\n            return\n\n        logger.info(\"Adding %d documents to Weaviate...\", len(chunks))\n\n        with self.client.batch as batch:\n            batch.batch_size = 100\n            for i, (vector, text) in enumerate(zip(embeddings, chunks)):\n                properties = {\n                    \"text\": text,\n                }\n\n                if metadata and i &lt; len(metadata):\n                    meta = metadata[i]\n                    if \"parent_text\" in meta:\n                        properties[\"parent_text\"] = meta[\"parent_text\"]\n\n                batch.add_data_object(\n                    data_object=properties,\n                    class_name=self.config.index_name,\n                    vector=vector,\n                    uuid=uuid.uuid4(),\n                )\n\n        logger.info(\"Successfully added documents to Weaviate.\")\n\n    def search(\n        self,\n        query_embedding: Any,\n        top_k: int = 4,\n        query_text: str = \"\",\n        **kwargs: Any,\n    ) -&gt; List[Tuple[str, float]]:\n        \"\"\"Search Weaviate using vector, BM25, or hybrid retrieval.\n\n        Args:\n            query_embedding: Query embedding used by vector and hybrid search.\n            top_k: Maximum number of chunks to return.\n            query_text: Raw query text used by BM25 and hybrid search.\n            **kwargs: Search options. Supported keys are ``strategy`` and\n                ``alpha``.\n\n        Returns:\n            List of ``(chunk_text, score)`` pairs.\n\n        Raises:\n            ValueError: If ``strategy`` is not ``\"vector\"``, ``\"bm25\"``, or\n                ``\"hybrid\"``.\n        \"\"\"\n        strategy = kwargs.get(\"strategy\", \"vector\")\n        alpha = kwargs.get(\"alpha\", 0.5)\n\n        query = self.client.query.get(self.config.index_name, [\"text\"])\n\n        if strategy == \"vector\":\n            vector = query_embedding.flatten().tolist()\n            query = query.with_near_vector(\n                {\n                    \"vector\": vector,\n                    \"certainty\": 0.0,\n                }\n            )\n            additional_fields = [\"certainty\", \"distance\"]\n\n        elif strategy == \"bm25\":\n            if not query_text:\n                logger.warning(\n                    \"BM25 search requested but no query text provided. Falling back to vector search.\"\n                )\n                return self.search(query_embedding, top_k, query_text, strategy=\"vector\")\n\n            query = query.with_bm25(query=query_text, properties=[\"text\"])\n            additional_fields = [\"score\"]\n\n        elif strategy == \"hybrid\":\n            if not query_text:\n                logger.warning(\n                    \"Hybrid search requested but no query text provided. Falling back to vector search.\"\n                )\n                return self.search(query_embedding, top_k, query_text, strategy=\"vector\")\n\n            vector = query_embedding.flatten().tolist()\n            query = query.with_hybrid(\n                query=query_text,\n                vector=vector,\n                alpha=alpha,\n                properties=[\"text\"],\n            )\n            additional_fields = [\"score\"]\n\n        else:\n            raise ValueError(f\"Unknown search strategy: {strategy}\")\n\n        response = query.with_limit(top_k).with_additional(additional_fields).do()\n\n        if \"errors\" in response:\n            logger.error(\"Weaviate search error: %s\", response[\"errors\"])\n            return []\n\n        results = []\n        data = response.get(\"data\", {}).get(\"Get\", {}).get(self.config.index_name, [])\n\n        for item in data:\n            text = item.get(\"text\")\n            additional = item.get(\"_additional\", {})\n\n            if strategy == \"vector\":\n                score = additional.get(\"certainty\", 0.0)\n            else:\n                score = float(additional.get(\"score\", 0.0))\n\n            results.append((text, score))\n\n        return results\n</code></pre>"},{"location":"reference/api/#hayagriva.core.vectorstores.WeaviateVectorStore.__init__","title":"<code>__init__(config)</code>","text":"<p>Connect to Weaviate and ensure the target class exists.</p> <p>Parameters:</p> Name Type Description Default <code>config</code> <code>WeaviateConfig</code> <p>Weaviate connection and class configuration.</p> required <p>Raises:</p> Type Description <code>MissingDependencyError</code> <p>If <code>weaviate-client</code> is not installed.</p> Source code in <code>hayagriva/core/vectorstores/weaviate.py</code> <pre><code>def __init__(self, config: WeaviateConfig) -&gt; None:\n    \"\"\"Connect to Weaviate and ensure the target class exists.\n\n    Args:\n        config: Weaviate connection and class configuration.\n\n    Raises:\n        MissingDependencyError: If ``weaviate-client`` is not installed.\n    \"\"\"\n    if importlib.util.find_spec(\"weaviate\") is None:\n        raise MissingDependencyError(\n            \"weaviate-client is required for Weaviate support. Install with `pip install weaviate-client`.\"\n        )\n    import weaviate\n\n    self.config = config\n    self.client = weaviate.Client(\n        url=config.url,\n        auth_client_secret=(\n            weaviate.auth.AuthApiKey(api_key=config.api_key) if config.api_key else None\n        ),\n    )\n\n    self._ensure_schema()\n</code></pre>"},{"location":"reference/api/#hayagriva.core.vectorstores.WeaviateVectorStore.add","title":"<code>add(embeddings, chunks, metadata=None)</code>","text":"<p>Add embedded chunks to Weaviate.</p> <p>Parameters:</p> Name Type Description Default <code>embeddings</code> <code>Any</code> <p>Two-dimensional array-like object of chunk embeddings.</p> required <code>chunks</code> <code>Sequence[str]</code> <p>Chunk text aligned with <code>embeddings</code>.</p> required <code>metadata</code> <code>Sequence[dict] | None</code> <p>Optional metadata dictionaries. <code>parent_text</code> is stored when present.</p> <code>None</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>If embeddings and chunks have different lengths.</p> Source code in <code>hayagriva/core/vectorstores/weaviate.py</code> <pre><code>def add(self, embeddings: Any, chunks: Sequence[str], metadata: Sequence[dict] | None = None) -&gt; None:\n    \"\"\"Add embedded chunks to Weaviate.\n\n    Args:\n        embeddings: Two-dimensional array-like object of chunk embeddings.\n        chunks: Chunk text aligned with ``embeddings``.\n        metadata: Optional metadata dictionaries. ``parent_text`` is stored\n            when present.\n\n    Raises:\n        ValueError: If embeddings and chunks have different lengths.\n    \"\"\"\n    if len(embeddings) != len(chunks):\n        raise ValueError(\"Embeddings and chunks length mismatch\")\n\n    if len(chunks) == 0:\n        return\n\n    logger.info(\"Adding %d documents to Weaviate...\", len(chunks))\n\n    with self.client.batch as batch:\n        batch.batch_size = 100\n        for i, (vector, text) in enumerate(zip(embeddings, chunks)):\n            properties = {\n                \"text\": text,\n            }\n\n            if metadata and i &lt; len(metadata):\n                meta = metadata[i]\n                if \"parent_text\" in meta:\n                    properties[\"parent_text\"] = meta[\"parent_text\"]\n\n            batch.add_data_object(\n                data_object=properties,\n                class_name=self.config.index_name,\n                vector=vector,\n                uuid=uuid.uuid4(),\n            )\n\n    logger.info(\"Successfully added documents to Weaviate.\")\n</code></pre>"},{"location":"reference/api/#hayagriva.core.vectorstores.WeaviateVectorStore.search","title":"<code>search(query_embedding, top_k=4, query_text='', **kwargs)</code>","text":"<p>Search Weaviate using vector, BM25, or hybrid retrieval.</p> <p>Parameters:</p> Name Type Description Default <code>query_embedding</code> <code>Any</code> <p>Query embedding used by vector and hybrid search.</p> required <code>top_k</code> <code>int</code> <p>Maximum number of chunks to return.</p> <code>4</code> <code>query_text</code> <code>str</code> <p>Raw query text used by BM25 and hybrid search.</p> <code>''</code> <code>**kwargs</code> <code>Any</code> <p>Search options. Supported keys are <code>strategy</code> and <code>alpha</code>.</p> <code>{}</code> <p>Returns:</p> Type Description <code>List[Tuple[str, float]]</code> <p>List of <code>(chunk_text, score)</code> pairs.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If <code>strategy</code> is not <code>\"vector\"</code>, <code>\"bm25\"</code>, or <code>\"hybrid\"</code>.</p> Source code in <code>hayagriva/core/vectorstores/weaviate.py</code> <pre><code>def search(\n    self,\n    query_embedding: Any,\n    top_k: int = 4,\n    query_text: str = \"\",\n    **kwargs: Any,\n) -&gt; List[Tuple[str, float]]:\n    \"\"\"Search Weaviate using vector, BM25, or hybrid retrieval.\n\n    Args:\n        query_embedding: Query embedding used by vector and hybrid search.\n        top_k: Maximum number of chunks to return.\n        query_text: Raw query text used by BM25 and hybrid search.\n        **kwargs: Search options. Supported keys are ``strategy`` and\n            ``alpha``.\n\n    Returns:\n        List of ``(chunk_text, score)`` pairs.\n\n    Raises:\n        ValueError: If ``strategy`` is not ``\"vector\"``, ``\"bm25\"``, or\n            ``\"hybrid\"``.\n    \"\"\"\n    strategy = kwargs.get(\"strategy\", \"vector\")\n    alpha = kwargs.get(\"alpha\", 0.5)\n\n    query = self.client.query.get(self.config.index_name, [\"text\"])\n\n    if strategy == \"vector\":\n        vector = query_embedding.flatten().tolist()\n        query = query.with_near_vector(\n            {\n                \"vector\": vector,\n                \"certainty\": 0.0,\n            }\n        )\n        additional_fields = [\"certainty\", \"distance\"]\n\n    elif strategy == \"bm25\":\n        if not query_text:\n            logger.warning(\n                \"BM25 search requested but no query text provided. Falling back to vector search.\"\n            )\n            return self.search(query_embedding, top_k, query_text, strategy=\"vector\")\n\n        query = query.with_bm25(query=query_text, properties=[\"text\"])\n        additional_fields = [\"score\"]\n\n    elif strategy == \"hybrid\":\n        if not query_text:\n            logger.warning(\n                \"Hybrid search requested but no query text provided. Falling back to vector search.\"\n            )\n            return self.search(query_embedding, top_k, query_text, strategy=\"vector\")\n\n        vector = query_embedding.flatten().tolist()\n        query = query.with_hybrid(\n            query=query_text,\n            vector=vector,\n            alpha=alpha,\n            properties=[\"text\"],\n        )\n        additional_fields = [\"score\"]\n\n    else:\n        raise ValueError(f\"Unknown search strategy: {strategy}\")\n\n    response = query.with_limit(top_k).with_additional(additional_fields).do()\n\n    if \"errors\" in response:\n        logger.error(\"Weaviate search error: %s\", response[\"errors\"])\n        return []\n\n    results = []\n    data = response.get(\"data\", {}).get(\"Get\", {}).get(self.config.index_name, [])\n\n    for item in data:\n        text = item.get(\"text\")\n        additional = item.get(\"_additional\", {})\n\n        if strategy == \"vector\":\n            score = additional.get(\"certainty\", 0.0)\n        else:\n            score = float(additional.get(\"score\", 0.0))\n\n        results.append((text, score))\n\n    return results\n</code></pre>"},{"location":"reference/api/#generation","title":"Generation","text":""},{"location":"reference/api/#hayagriva.core.generators.GroqGenerator","title":"<code>hayagriva.core.generators.GroqGenerator</code>","text":"<p>Generator backend using Groq chat completions.</p> <p>Parameters:</p> Name Type Description Default <code>api_key</code> <code>str</code> <p>Groq API key.</p> required <code>model</code> <code>str</code> <p>Groq chat model name.</p> <code>'llama-3.1-8b-instant'</code> <p>Attributes:</p> Name Type Description <code>client</code> <p>Groq SDK client.</p> <code>model</code> <p>Model name used for generation.</p> <p>Raises:</p> Type Description <code>GenerationError</code> <p>If <code>api_key</code> is missing.</p> Source code in <code>hayagriva/core/generators/groq.py</code> <pre><code>class GroqGenerator:\n    \"\"\"Generator backend using Groq chat completions.\n\n    Args:\n        api_key: Groq API key.\n        model: Groq chat model name.\n\n    Attributes:\n        client: Groq SDK client.\n        model: Model name used for generation.\n\n    Raises:\n        GenerationError: If ``api_key`` is missing.\n    \"\"\"\n\n    def __init__(self, api_key: str, model: str = \"llama-3.1-8b-instant\"):\n        \"\"\"Create a Groq chat-completion generator.\n\n        Args:\n            api_key: Groq API key.\n            model: Groq model name.\n\n        Raises:\n            GenerationError: If ``api_key`` is missing.\n        \"\"\"\n        if not api_key:\n            raise GenerationError(\"Groq API key is missing.\")\n\n        self.client = Groq(api_key=api_key)\n        self.model = model\n\n    def generate(self, prompt: str) -&gt; str:\n        \"\"\"Generate an answer for a completed prompt.\n\n        Args:\n            prompt: Full prompt containing instructions, context, and question.\n\n        Returns:\n            Generated assistant text.\n\n        Raises:\n            GenerationError: If the Groq API call fails.\n        \"\"\"\n        try:\n            response = self.client.chat.completions.create(\n                model=self.model,\n                messages=[{\"role\": \"user\", \"content\": prompt}],\n                temperature=0.7,\n            )\n            return response.choices[0].message.content\n\n        except Exception as exc:\n            raise GenerationError(f\"Groq generation failed: {exc}\") from exc\n</code></pre>"},{"location":"reference/api/#hayagriva.core.generators.GroqGenerator.__init__","title":"<code>__init__(api_key, model='llama-3.1-8b-instant')</code>","text":"<p>Create a Groq chat-completion generator.</p> <p>Parameters:</p> Name Type Description Default <code>api_key</code> <code>str</code> <p>Groq API key.</p> required <code>model</code> <code>str</code> <p>Groq model name.</p> <code>'llama-3.1-8b-instant'</code> <p>Raises:</p> Type Description <code>GenerationError</code> <p>If <code>api_key</code> is missing.</p> Source code in <code>hayagriva/core/generators/groq.py</code> <pre><code>def __init__(self, api_key: str, model: str = \"llama-3.1-8b-instant\"):\n    \"\"\"Create a Groq chat-completion generator.\n\n    Args:\n        api_key: Groq API key.\n        model: Groq model name.\n\n    Raises:\n        GenerationError: If ``api_key`` is missing.\n    \"\"\"\n    if not api_key:\n        raise GenerationError(\"Groq API key is missing.\")\n\n    self.client = Groq(api_key=api_key)\n    self.model = model\n</code></pre>"},{"location":"reference/api/#hayagriva.core.generators.GroqGenerator.generate","title":"<code>generate(prompt)</code>","text":"<p>Generate an answer for a completed prompt.</p> <p>Parameters:</p> Name Type Description Default <code>prompt</code> <code>str</code> <p>Full prompt containing instructions, context, and question.</p> required <p>Returns:</p> Type Description <code>str</code> <p>Generated assistant text.</p> <p>Raises:</p> Type Description <code>GenerationError</code> <p>If the Groq API call fails.</p> Source code in <code>hayagriva/core/generators/groq.py</code> <pre><code>def generate(self, prompt: str) -&gt; str:\n    \"\"\"Generate an answer for a completed prompt.\n\n    Args:\n        prompt: Full prompt containing instructions, context, and question.\n\n    Returns:\n        Generated assistant text.\n\n    Raises:\n        GenerationError: If the Groq API call fails.\n    \"\"\"\n    try:\n        response = self.client.chat.completions.create(\n            model=self.model,\n            messages=[{\"role\": \"user\", \"content\": prompt}],\n            temperature=0.7,\n        )\n        return response.choices[0].message.content\n\n    except Exception as exc:\n        raise GenerationError(f\"Groq generation failed: {exc}\") from exc\n</code></pre>"},{"location":"reference/api/#ingestion","title":"Ingestion","text":""},{"location":"reference/api/#hayagriva.ingestion.loaders.load_texts","title":"<code>hayagriva.ingestion.loaders.load_texts(docs)</code>","text":"<p>Validate and normalize raw document strings.</p> <p>Parameters:</p> Name Type Description Default <code>docs</code> <code>Iterable[str]</code> <p>Iterable of candidate document strings.</p> required <p>Returns:</p> Type Description <code>List[str]</code> <p>Non-empty document strings.</p> <p>Raises:</p> Type Description <code>IngestionError</code> <p>If no valid document strings are provided.</p> Source code in <code>hayagriva/ingestion/loaders.py</code> <pre><code>def load_texts(docs: Iterable[str]) -&gt; List[str]:\n    \"\"\"Validate and normalize raw document strings.\n\n    Args:\n        docs: Iterable of candidate document strings.\n\n    Returns:\n        Non-empty document strings.\n\n    Raises:\n        IngestionError: If no valid document strings are provided.\n    \"\"\"\n\n    return ensure_texts(docs)\n</code></pre>"},{"location":"reference/api/#hayagriva.ingestion.loaders.load_from_paths","title":"<code>hayagriva.ingestion.loaders.load_from_paths(paths)</code>","text":"<p>Load UTF-8 text from files or directories.</p> <p>Parameters:</p> Name Type Description Default <code>paths</code> <code>Iterable[str]</code> <p>Iterable of file or directory paths. Directories are searched recursively for <code>*.txt</code> files.</p> required <p>Returns:</p> Type Description <code>List[str]</code> <p>Text content read from all matching files.</p> <p>Raises:</p> Type Description <code>IngestionError</code> <p>If a path cannot be read or does not exist.</p> Source code in <code>hayagriva/ingestion/loaders.py</code> <pre><code>def load_from_paths(paths: Iterable[str]) -&gt; List[str]:\n    \"\"\"Load UTF-8 text from files or directories.\n\n    Args:\n        paths: Iterable of file or directory paths. Directories are searched\n            recursively for ``*.txt`` files.\n\n    Returns:\n        Text content read from all matching files.\n\n    Raises:\n        IngestionError: If a path cannot be read or does not exist.\n    \"\"\"\n\n    try:\n        return read_text_files(paths)\n    except OSError as exc:  # pylint: disable=broad-except\n        raise IngestionError(f\"Failed to read paths: {exc}\") from exc\n</code></pre>"}]}