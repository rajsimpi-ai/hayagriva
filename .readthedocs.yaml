version: 2

build:
  os: "ubuntu-24.04"
  tools:
    python: "3.12"

# Use MkDocs instead of Sphinx
mkdocs:
  configuration: mkdocs.yml

python:
  install:
    - requirements: requirements.txt
    - method: pip
      path: .
